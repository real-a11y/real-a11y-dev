  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Accessibility Snapshots"
        paragraph "Accessibility Snapshots"
      link "Snapshots vs. Other Tools"
        paragraph "Snapshots vs. Other Tools"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "Headings View"
        paragraph "Headings View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
        link "@real-a11y-dev/testing"
          heading "@real-a11y-dev/testing" (level 3)
        button "toggle section"
      link "Snapshots"
        paragraph "Snapshots"
      link "Assertions"
        paragraph "Assertions"
      link "Matchers"
        paragraph "Matchers"
      link "Flow API"
        paragraph "Flow API"
      link "Playwright adapter"
        paragraph "Playwright adapter"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "auditSnapshot(root, options?)"
      listitem
        link "outlineSnapshot(root)"
      listitem
        link "tabSequenceSnapshot(root)"
      listitem
        link "Determinism"
      listitem
        link "See also"
  main
    heading "Snapshots" (level 1)
    paragraph "Deterministic string representations of the accessibility tree. Safe to commit — the same DOM always produces the same string."
    paragraph "Part of . For the ergonomic form, see ."
      link "@real-a11y-dev/testing"
        code "@real-a11y-dev/testing"
      code "expect(...).toMatchSnapshot()"
      link "Matchers"
    heading "auditSnapshot(root, options?)" (level 2)
      code "auditSnapshot(root, options?)"
    paragraph "Returns a formatted string of the full semantic tree."
    blockquote
      paragraph "Using the entry? renders the with cleaner snapshot tooling. returns a raw string you can assert on or write to a file; returns a serializer-boxed value for native . See ."
        link "matchers"
        code "a11ySnapshot()"
        strong "same tree"
        code "auditSnapshot"
        code "a11ySnapshot"
        code "toMatchSnapshot()"
        link "a11ySnapshot vs auditSnapshot"
          code "a11ySnapshot"
          code "auditSnapshot"
    button "Copy Code"
      code
    paragraph
      strong "Options:"
    table
      rowgroup
        row
          rowheader "Option"
          rowheader "Type"
          rowheader "Default"
          rowheader "Description"
      rowgroup
        row
          cell "mode"
            code "mode"
          cell ""a11y" | "dom""
            code ""a11y" | "dom""
          cell ""a11y""
            code ""a11y""
          cell "Tree extraction mode."
        row
          cell "redact"
            code "redact"
          cell "RegExp[]"
            code "RegExp[]"
          cell "[]"
            code "[]"
          cell "Patterns replaced with [redacted] in names and text. Use this to keep snapshots deterministic."
            code "[redacted]"
    paragraph "Example output:"
    button "Copy Code"
      code
    heading "Using redact" (level 3)
      code "redact"
    paragraph "takes an array of regexes. Any substring of an that matches one is replaced with the literal placeholder . The element, its role, and its position are untouched — only the volatile text inside its name is masked."
      code "redact"
      strong "accessible name"
      code "[REDACTED]"
    heading "Without redact — the snapshot flakes" (level 4)
      code "redact"
    paragraph "A relative timestamp in an changes between runs, so the committed snapshot fails even though nothing actually regressed:"
      code "aria-label"
    button "Copy Code"
      code
    button "Copy Code"
      code
    heading "With redact — stable structure, masked text" (level 4)
      code "redact"
    button "Copy Code"
      code
    button "Copy Code"
      code
    paragraph "You still catch real regressions — if the button lost its label the name would change to — you just stop catching "the clock advanced.""
      code """"
    heading "Realistic patterns" (level 4)
    paragraph "Most flakes come from a handful of sources. Pass a regex for each noisy bit:"
    button "Copy Code"
      code
    paragraph "The redaction happens name computation — so an becomes in the snapshot string without changing the underlying tree. The element still appears, its role and structure still matter."
      strong "after"
      code "aria-label="Updated 2 minutes ago""
      code "[REDACTED]"
    paragraph "Define one shared list and reuse it across the suite so every snapshot masks the same noise consistently:"
    button "Copy Code"
      code
    paragraph "How works — and two gotchas"
      code "redact"
    list
      listitem "It runs on the , at serialization time. Roles, heading levels, and tree shape are never modified — changes the snapshot , not the extraction."
        strong "accessible name only"
        code "redact"
        emphasis "string"
      listitem "Patterns apply , cumulatively."
        strong "in order"
      listitem "Internally each pattern runs , and without only replaces the match — so with masks just the first date. masks both."
        strong "Add the flag if a name can contain more than one match."
          code "g"
        code "name.replace(pattern, "[REDACTED]")"
        code "String.replace"
        code "/g"
        emphasis "first"
        code ""Created 2026-01-01, edited 2026-02-02""
        code "/\d{4}-\d{2}-\d{2}/"
        code "/\d{4}-\d{2}-\d{2}/g"
      listitem "Masking a whole name blinds the snapshot to real regressions in it — remove the clock, not the label."
        strong "Redact the narrowest thing that's actually volatile."
    blockquote
      paragraph "Available everywhere snapshots are: and the matcher in jsdom, and the — which marshals each across the browser boundary for you."
        code "auditSnapshot"
        link "a11ySnapshot"
          code "a11ySnapshot"
        link "Playwright adapter"
        code "RegExp"
    heading "outlineSnapshot(root)" (level 2)
      code "outlineSnapshot(root)"
    paragraph "Returns a string of the heading outline only — useful for structure audits."
    button "Copy Code"
      code
    paragraph "Example output:"
    button "Copy Code"
      code
    heading "tabSequenceSnapshot(root)" (level 2)
      code "tabSequenceSnapshot(root)"
    paragraph "Returns the tab sequence as a numbered list."
    button "Copy Code"
      code
    paragraph "Example output:"
    button "Copy Code"
      code
    heading "Determinism" (level 2)
    paragraph "All three helpers return the for the same DOM — on every run, on every machine. No timestamps, no generated IDs, no ordering surprises. That's the property that makes safe in CI without flakes. For genuinely variable content, use ."
      strong "same string"
      code "toMatchSnapshot()"
      link "redact"
        code "redact"
    heading "See also" (level 2)
    list
      listitem "— serializer so renders the tree directly"
        link "Matchers"
        code "a11ySnapshot()"
        code "expect(el).toMatchSnapshot()"
      listitem "— the same snapshots against a real browser"
        link "Playwright adapter"
      listitem "— wiring a snapshot generator into a PR workflow"
        link "CI Diff Bot recipe"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous page@real-a11y-dev/testing"
      link "Next pageAssertions"