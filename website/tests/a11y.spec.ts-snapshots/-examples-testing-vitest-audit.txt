navigation "On this page"
  heading "On this page" (level 2)
  list
    listitem
      link "What it shows"
    listitem
      link "Run it locally"
    listitem
      link "Key code"
    listitem
      link "See also"
main
  heading "Example: Vitest Integration" (level 1)
  paragraph "Demonstrates in a Vitest + jsdom test suite — snapshots, structural assertions, and the chain."
    code "@real-a11y-dev/testing"
    code "flow()"
  paragraph
    strong "Source:"
    link "examples/testing-vitest/"
      code "examples/testing-vitest/"
  heading "What it shows" (level 2)
  list
    listitem "as a Vitest snapshot"
      code "treeSnapshot()"
    listitem "in a form component test"
      code "assertNoUnlabeledInteractive()"
    listitem "checking page-level heading structure"
      code "assertHeadingOrder()"
    listitem "verifying //"
      code "assertLandmarkStructure()"
      code "main"
      code "header"
      code "footer"
    listitem "chaining through a combobox interaction"
      code "flow()"
    listitem "+ for structure tests"
      code "outlineSnapshot()"
      code "tabSequenceSnapshot()"
  heading "Run it locally" (level 2)
  button "Copy Code"
  code
  heading "Key code" (level 2)
  heading "Snapshot test" (level 3)
  button "Copy Code"
  code
  heading "Structural assertions" (level 3)
  button "Copy Code"
  code
  heading "Flow chain — combobox interaction" (level 3)
  button "Copy Code"
  code
  heading "Tab sequence test" (level 3)
  button "Copy Code"
  code
  heading "See also" (level 2)
  paragraph "jsdom runs the same audit engine as the browser, but it can't audit a real running app or gate CI on a live page set. For that:"
  list
    listitem "— the adapter runs these same assertions against a real browser page."
      strong
        link "Playwright E2E"
      code "@real-a11y-dev/testing/playwright"
      code "attach()"
    listitem "— / / run findings-aware audits over the page set in , ideal for CI."
      strong
        link "@real-a11y-dev/cli"
          code "@real-a11y-dev/cli"
      code "real-a11y audit"
      code "snapshot"
      code "diff"
      code "a11y.config.json"
    listitem "— the same audit engine exposed to AI agents for agent-driven checks."
      strong
        link "@real-a11y-dev/mcp"
          code "@real-a11y-dev/mcp"
contentinfo
  link "Edit this page on GitHub"
  paragraph "Last updated:"
    time "[redacted]"
  navigation "Pager"
    link "Next pageGetting Started"