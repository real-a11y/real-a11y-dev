  h2 On this page
h1 Matchers
  h2 Setup
    h3 Vitest
    h3 Jest
  h2 Assertion matchers
  h2 toBeValidA11yTree()
  h2 toHaveTabSequence(expected)
  h2 toMatchA11yContract(contract, options?)
  h2 boxedTreeSnapshot(root, options?) — snapshot serializer
    h3 boxedTreeSnapshot vs treeSnapshot
    h3 Modal scoping in snapshots
  h2 Rules, contracts, and snapshots — which to reach for
    h3 Rules and contracts catch disjoint bugs
    h3 In practice
  h2 Vitest vs Jest type augmentation
  h2 See it running
  h2 See also