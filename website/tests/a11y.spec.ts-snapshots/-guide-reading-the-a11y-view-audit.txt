navigation "On this page"
  heading "On this page" (level 2)
  list
    listitem
      link "What you see"
    listitem
      link "How the A11y view differs from the DOM view"
    listitem
      link "What to look for"
    listitem
      link "Reading a well-structured tree"
    listitem
      link "Quick sanity checks"
main
  heading "Reading the A11y View" (level 1)
  blockquote
    paragraph "The A11y view is the same tree across every Real A11y surface — the , the , the , the string from the testing package, the command in the CLI, and the tool over MCP. This page applies to all of them."
      link "Chrome extension"
      code "<SemanticNavigator />"
      link "React component"
      link "Storybook addon"
      link "treeSnapshot()"
        code "treeSnapshot()"
      link "real-a11y tree"
        code "real-a11y tree"
      link "get_semantic_tree"
        code "get_semantic_tree"
  paragraph "The A11y view shows you what assistive technology actually perceives — screen readers, voice control software, switch devices, and the browser's own accessibility APIs. Not what you wrote in HTML, but what users with disabilities actually experience when they interact with your page."
  paragraph "It answers the question:"
    strong ""If someone couldn't see this, what would they hear?""
  paragraph "This is the most important view."
  img "Visual view: a website with blue header, teal nav, purple main content, amber aside, and slate footer arranged as a spatial layout."
  img "A11y tree view: the same sections rearranged as an indented tree — banner, navigation, main with headings, paragraphs, inputs and button, complementary, and contentinfo."
  paragraph "Left: what sighted users see. Right: what assistive technology reads. Same content — different structure."
  separator
  heading "What you see" (level 2)
  paragraph "Each node represents one meaningful element in the accessibility tree. For each one, you'll see:"
  list
    listitem "— What kind of thing it is. , , , , , , … This comes from the HTML tag or an explicit attribute."
      strong "Role"
      code "button"
      code "heading"
      code "textbox"
      code "listitem"
      code "dialog"
      code "navigation"
      code "region"
      code "role"
    listitem "— What it's called. The label that gets announced. This is what makes the difference between hearing and hearing ."
      strong "Name"
      emphasis ""button""
      emphasis ""Submit order""
    listitem "— What condition it's in. Checked or unchecked. Expanded or collapsed. Disabled. Selected. Required."
      strong "State"
  separator
  heading "How the A11y view differs from the DOM view" (level 2)
  paragraph "The DOM view shows every HTML element. The A11y view filters and transforms:"
  list
    listitem "A used only for layout with no role or text shows up in DOM but not in A11y. Same for , empty containers, and elements with or ."
      strong "Presentational elements disappear."
      code "<div>"
      code "<span>"
      code "role="presentation""
      code "aria-hidden="true""
    listitem "A and a element look different in DOM but both appear as in A11y."
      strong "Semantics win over markup."
      code "<div role="navigation">"
      code "<nav>"
      code "navigation"
    listitem "The name shown is the of the full accessible name algorithm — not just the attribute, but the winner of a priority contest between label elements, , , , and text content."
      strong "Names are computed, not just copied."
      emphasis "result"
      code "aria-label"
      code "aria-labelledby"
      code "aria-label"
      code "title"
  separator
  heading "What to look for" (level 2)
  heading "Interactive elements with no name" (level 3)
  paragraph "This is the single most common issue. An icon-only button, a link with no text, a form input with no visible label — these show up in the A11y view with a role but an empty name."
  button "Copy Code"
  code
  paragraph "A screen reader user hears: . No context. They have to guess what it does, or give up."
    emphasis ""button""
  paragraph "Add visible text, an , or link the element to a visible label with . If the button has an icon, the icon needs an or ."
    strong "The fix:"
    code "aria-label"
    code "aria-labelledby"
    code "alt"
    code "aria-label"
  heading "Everything is generic" (level 3)
    code "generic"
  paragraph "The role is what and get when they have no semantic role. If your A11y tree looks like:"
    code "generic"
    code "<div>"
    code "<span>"
  button "Copy Code"
  code
  paragraph "…the screen reader has no structural context around that button. No landmarks to jump to, no headings to navigate by. Sighted users can scan visually; screen reader users navigate by structure."
  paragraph "Use landmark elements (, , , ) and semantic elements (–, , , ) to give the tree shape."
    strong "The fix:"
    code "<main>"
    code "<nav>"
    code "<header>"
    code "<aside>"
    code "<h1>"
    code "<h6>"
    code "<ul>"
    code "<button>"
    code "<a>"
  heading "Heading levels that jump" (level 3)
  paragraph "Headings are how screen reader users navigate long pages — they Tab through headings the same way sighted users scan titles. If you jump from an directly to an , users navigating by heading lose their place in the hierarchy."
    code "h1"
    code "h3"
  button "Copy Code"
  code
  paragraph "Heading levels should only increase by one at a time. Use CSS to control visual size — don't pick a heading level based on how big it looks."
    strong "The fix:"
  heading "Images with no name" (level 3)
  paragraph "An without an attribute, or with when the image conveys meaning, shows up in the A11y tree with no name. Decorative images should genuinely be decorative — if it communicates content (a product photo, a chart, a badge), it needs a description."
    code "<img>"
    code "alt"
    code "alt="""
  heading "ARIA states that don't match reality" (level 3)
  paragraph "An accordion with is good. An that never changes when the accordion opens is a bug — the A11y tree will say "collapsed" while the panel is visually open. The A11y view will catch this: look at the state and ask whether it matches what you see on screen."
    code "<button>"
    code "aria-expanded"
    code "aria-expanded"
  heading "Elements that should be in the tree but aren't" (level 3)
  paragraph "If a custom widget isn't showing up with the right role — or isn't showing up at all — it may be , visually hidden but also hidden from AT, or built with markup that doesn't carry semantic meaning. The A11y view makes absences visible."
    code "aria-hidden"
  separator
  heading "Reading a well-structured tree" (level 2)
  paragraph "A healthy A11y tree looks something like this:"
  button "Copy Code"
  code
  paragraph "Landmarks structure the page. Headings create hierarchy. Interactive elements have meaningful names. Nothing generic where something semantic belongs."
  separator
  heading "Quick sanity checks" (level 2)
  paragraph "Run through these any time you open the A11y view on an unfamiliar component:"
  list
    listitem "Do all interactive elements have a name?"
    listitem "Is the heading outline logical — one , no skipped levels?"
      code "h1"
    listitem "Do landmark regions cover the whole page (no orphaned content outside , , etc.)?"
      code "main"
      code "nav"
    listitem "Do ARIA states on collapsibles, dialogs, and toggles match the current visual state?"
    listitem "Are there any containers where a landmark or semantic element belongs?"
      code "generic"
contentinfo
  link "Edit this page on GitHub"
  paragraph "Last updated:"
    time "[redacted]"
  navigation "Pager"
    link "Previous pageDOM View"
    link "Next pageHeadings View"