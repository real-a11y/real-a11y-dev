navigation "On this page"
  heading "On this page" (level 2)
  list
    listitem
      link "What it shows"
    listitem
      link "Run it locally"
    listitem
      link "Key code"
main
  heading "Example: Vanilla / Vite" (level 1)
  paragraph "A minimal Vite + TypeScript application demonstrating as a floating dev-tools overlay."
    code "@real-a11y-dev/inspector"
  paragraph
    strong "Source:"
    link "examples/vanilla/"
      code "examples/vanilla/"
  heading "What it shows" (level 2)
  list
    listitem "Mounting the panel into a floating container via Shadow DOM"
    listitem "Switching between A11y and DOM modes at runtime"
    listitem "Using to narrow the observed subtree to just a dialog"
      code "setRoot()"
    listitem "Calling after a programmatic DOM update"
      code "refresh()"
    listitem "— hover a tree node to highlight the corresponding DOM element"
      code "highlightOnHover"
  heading "Run it locally" (level 2)
  button "Copy Code"
  code
  paragraph "Opens at (Vite's default port)."
    code "http://localhost:5173"
  heading "Key code" (level 2)
  button "Copy Code"
  code
contentinfo
  link "Edit this page on GitHub"
  paragraph "Last updated:"
    time "[redacted]"
  navigation "Pager"
    link "Next pageGetting Started"