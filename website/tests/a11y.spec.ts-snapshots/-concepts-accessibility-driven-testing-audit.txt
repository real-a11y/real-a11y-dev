  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Concepts"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Concepts"
        heading "Concepts" (level 2)
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Accessibility-Driven Development"
        paragraph "Accessibility-Driven Development"
      link "Accessibility-Driven Testing"
        paragraph "Accessibility-Driven Testing"
      link "Accessibility Snapshots"
        paragraph "Accessibility Snapshots"
      link "Snapshots vs. Other Tools"
        paragraph "Snapshots vs. Other Tools"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "Headings View"
        paragraph "Headings View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
        link "@real-a11y-dev/testing"
          heading "@real-a11y-dev/testing" (level 3)
        button "toggle section"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "The gap it closes"
      listitem
        link "Two ways to pin the tree"
      listitem
        link "It runs where your tests already run"
      listitem
        link "It composes — it doesn't replace"
      listitem
        link "Closing the loop"
      listitem
        link "Start here"
  main
    heading "Accessibility-Driven Testing" (level 1)
    paragraph "Test-driven development treats a failing test as a contract. treats the the same way: the roles, names, heading outline, and focus order your users perceive become a first-class artifact your suite asserts on — and a regression in it fails the build like any other."
      strong "Accessibility-Driven Testing"
      strong "accessibility tree"
    heading "The gap it closes" (level 2)
    paragraph "Look at the accessibility tools a mature team already runs, and there's a hole in the middle:"
    list
      listitem "(axe, jest-axe) answer — they don't answer A button that lost its label, a heading outline that skipped a level, a new focus trap — none are rule violations, and all sail straight through."
        strong "Rule engines"
        emphasis ""does this violate a WCAG rule right now?""
        emphasis ""did the structure change?""
      listitem "miss semantics entirely. A real swapped for a styled moves and doesn't dent a screenshot — but it's gone from the accessibility tree."
        strong "DOM and visual snapshots"
        code "<button>"
        code "<div>"
        strong "zero pixels"
      listitem "catches these, but it . Nothing stops the regression from landing between audits."
        strong "Manual screen-reader testing"
        strong "doesn't run in CI"
    paragraph "Accessibility-Driven Testing fills that middle: it makes a thing your pipeline checks automatically."
      emphasis "structure"
    heading "Two ways to pin the tree" (level 2)
    paragraph "gives you two complementary modes:"
      link "@real-a11y-dev/testing"
        code "@real-a11y-dev/testing"
    list
      listitem "/ / serialize the tree to a deterministic string you commit and diff in review. Rename a wrapper class and it's byte-identical; drop a label or skip a heading and it changes immediately. (The concept, in depth: .)"
        strong "Snapshots — catch regressions."
        link "auditSnapshot"
          code "auditSnapshot"
        code "outlineSnapshot"
        code "tabSequenceSnapshot"
        link "Accessibility Snapshots"
      listitem ", , , … throw descriptive errors on broken structure. Prefer style? The same checks ship as : , , ."
        strong "Assertions — enforce invariants."
        link "assertNoUnlabeledInteractive"
          code "assertNoUnlabeledInteractive"
        code "assertHeadingOrder"
        code "assertDialogsLabeled"
        code "expect"
        link "matchers"
        code "toHaveNoUnlabeledInteractive()"
        code "toHaveValidHeadingOrder()"
        code "toHaveTabSequence([...])"
    paragraph "For flows, asserts about the tree an interaction — open a menu, submit a form — so you can lock in behavior like modal scoping and focus traps, not just static markup."
      link "flow()"
        code "flow()"
      emphasis "after"
    heading "It runs where your tests already run" (level 2)
    paragraph "The distinctive part: the same tree computes in — a millisecond-fast Vitest/Jest unit test, no browser — in a real browser via the (). One format, from the fastest unit test to full E2E."
      strong "jsdom"
      strong "and"
      link "Playwright adapter"
      code "attach(page)"
    button "Copy Code"
      code
    paragraph "On every PR, the posts the tree diff as a comment — so a change to what assistive tech perceives is as visible in review as a change to any other file."
      link "CI diff bot"
    heading "It composes — it doesn't replace" (level 2)
    paragraph "Run for rule compliance, an accessibility snapshot for structural regression, and a real screen reader before you ship. Accessibility-Driven Testing owns exactly one slice — — and it's the slice nothing else in CI was watching. None of these subsumes the others."
      code "toHaveNoViolations()"
      emphasis "structure, and whether it changed over time"
    heading "Closing the loop" (level 2)
    paragraph "Because it's one engine, the tree is the same everywhere it appears. A QA teammate can straight from the Chrome extension and paste it into a bug — and that paste is byte-identical to , so a developer drops it straight into a fixture. Inspect in dev, report from the extension, assert in CI — one artifact, end to end. Which is the other half of the practice: ."
      link "copy the tree"
      code "auditSnapshot()"
      code "toMatchSnapshot()"
      link "Accessibility-Driven Development"
        strong "Accessibility-Driven Development"
    heading "Start here" (level 2)
    list
      listitem "· · · ·"
        link "Snapshots"
        link "Assertions"
        link "Matchers"
        link "Flow"
        link "Playwright"
      listitem "— tree diffs on every PR"
        link "CI Diff Bot recipe"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous pageAccessibility-Driven Development"
      link "Next pageAccessibility Snapshots"