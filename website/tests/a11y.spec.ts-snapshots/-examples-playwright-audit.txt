navigation "On this page"
  heading "On this page" (level 2)
  list
    listitem
      link "What it shows"
    listitem
      link "Run it locally"
    listitem
      link "Setup"
    listitem
      link "Good fixture — assertions pass"
    listitem
      link "Bad fixture — assertions throw"
    listitem
      link "Narrowing the audit root"
    listitem
      link "Snapshot test"
    listitem
      link "How the adapter works"
    listitem
      link "See also"
main
  heading "Example: Playwright E2E" (level 1)
  paragraph "Demonstrates — the adapter — against real fixture pages in a Playwright test suite."
    code "@real-a11y-dev/testing/playwright"
    code "attach()"
  paragraph
    strong "Source:"
    link "packages/testing/e2e/"
      code "packages/testing/e2e/"
  heading "What it shows" (level 2)
  list
    listitem "injecting the audit engine into a real browser page"
      code "attach(page)"
    listitem "producing a stable string for snapshot tests"
      code "treeSnapshot()"
    listitem "verifying heading structure"
      code "outlineSnapshot()"
    listitem "verifying tab order"
      code "tabSequenceSnapshot()"
    listitem "All four assertions (, , , ) passing on a well-structured page"
      code "assertHeadingOrder"
      code "assertNoUnlabeledInteractive"
      code "assertLandmarkStructure"
      code "assertDialogsLabeled"
    listitem "The same assertions on a broken page — error messages surfaced in the test runner"
      strong "throwing"
    listitem "narrowing the audit to a specific subtree"
      code "rootSelector"
  heading "Run it locally" (level 2)
  button "Copy Code"
  code
  heading "Setup" (level 2)
  button "Copy Code"
  code
  heading "Good fixture — assertions pass" (level 2)
  button "Copy Code"
  code
  paragraph "The good fixture has:"
  list
    listitem "One with two sections (no skipped levels)"
      code "<h1>"
      code "<h2>"
    listitem "Labeled , , landmarks"
      code "<header>"
      code "<main>"
      code "<footer>"
    listitem "A form with proper elements on every input"
      code "<label>"
    listitem "A with"
      code "<dialog>"
      code "aria-labelledby"
  heading "Bad fixture — assertions throw" (level 2)
  button "Copy Code"
  code
  paragraph "The bad fixture has:"
  list
    listitem "No (starts at , then jumps to )"
      code "<h1>"
      code "<h2>"
      code "<h4>"
    listitem "No landmark"
      code "<main>"
    listitem "An unlabeled and unlabeled"
      code "<button>"
      code "<input>"
    listitem "An open with no or"
      code "<dialog>"
      code "aria-label"
      code "aria-labelledby"
  heading "Narrowing the audit root" (level 2)
  button "Copy Code"
  code
  heading "Snapshot test" (level 2)
  button "Copy Code"
  code
  paragraph "Commit output with to catch unintended accessibility regressions in CI:"
    code "treeSnapshot()"
    code "toMatchSnapshot()"
  button "Copy Code"
  code
  heading "How the adapter works" (level 2)
  button "Copy Code"
  code
  paragraph "The bundle (30 KB) is read from disk once and cached in the Node process — subsequent calls add no I/O overhead."
    code "attach()"
  heading "See also" (level 2)
  list
    listitem "— / run findings-aware CI audits over a whole page set defined in , no test harness required."
      strong
        link "@real-a11y-dev/cli"
          code "@real-a11y-dev/cli"
      code "real-a11y snapshot"
      code "real-a11y diff"
      code "a11y.config.json"
    listitem "— the same audit engine exposed to AI agents for agent-driven accessibility checks."
      strong
        link "@real-a11y-dev/mcp"
          code "@real-a11y-dev/mcp"
contentinfo
  link "Edit this page on GitHub"
  paragraph "Last updated:"
    time "[redacted]"
  navigation "Pager"
    link "Next pageGetting Started"