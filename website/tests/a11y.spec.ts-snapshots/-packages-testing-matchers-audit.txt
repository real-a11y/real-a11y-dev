  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Accessibility Snapshots"
        paragraph "Accessibility Snapshots"
      link "Snapshots vs. Other Tools"
        paragraph "Snapshots vs. Other Tools"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "Headings View"
        paragraph "Headings View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
        link "@real-a11y-dev/testing"
          heading "@real-a11y-dev/testing" (level 3)
        button "toggle section"
      link "Snapshots"
        paragraph "Snapshots"
      link "Assertions"
        paragraph "Assertions"
      link "Matchers"
        paragraph "Matchers"
      link "Flow API"
        paragraph "Flow API"
      link "Playwright adapter"
        paragraph "Playwright adapter"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
      link "@real-a11y-dev/mcp"
        paragraph "@real-a11y-dev/mcp"
      link "@real-a11y-dev/cli"
        paragraph "@real-a11y-dev/cli"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Authenticated Pages"
        paragraph "Authenticated Pages"
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "Setup"
      listitem
        link "Assertion matchers"
      listitem
        link "toBeValidA11yTree()"
      listitem
        link "toHaveTabSequence(expected)"
      listitem
        link "a11ySnapshot(root, options?) — snapshot serializer"
      listitem
        link "Vitest vs Jest type augmentation"
      listitem
        link "See it running"
      listitem
        link "See also"
  main
    heading "Matchers" (level 1)
    paragraph "An ergonomic layer over the and — the same checks, but read as native matchers with negation and clean failure messages. This is the jest-axe-style surface of the package."
      code "expect"
      link "assertions"
      link "snapshots"
      code ".not"
    paragraph "Shipped from a separate, opt-in entry so the main package stays side-effect-free:"
    button "Copy Code"
      code
    blockquote
      paragraph "jest-axe runs a WCAG and reports violations. These matchers assert and capture of the semantic tree — including focus order and modal scoping, which a rule engine doesn't model. The two compose well in one suite. See for the distinction."
        strong "Not the same as jest-axe."
        strong "rule engine"
        strong "structure"
        strong "snapshots"
        link "Accessibility Snapshots"
    heading "Setup" (level 2)
    paragraph "Registration is opt-in (the jest-axe pattern): call once from a setup file. Nothing runs on import."
      code "registerA11yMatchers(expect)"
    heading "Vitest" (level 3)
    button "Copy Code"
      code
    button "Copy Code"
      code
    heading "Jest" (level 3)
    button "Copy Code"
      code
    button "Copy Code"
      code
    paragraph "TypeScript module resolution (Jest + ts-jest)"
    paragraph "The subpath is exposed through the package field, so TypeScript must read it. Use (or / ) — the legacy resolver ignores and the import won't resolve. also needs under Node16."
      code "/matchers"
      code "exports"
      code ""moduleResolution": "Node16""
      code ""NodeNext""
      code ""Bundler""
      code ""node""
      code "exports"
      code "ts-jest"
      code ""isolatedModules": true"
    heading "Assertion matchers" (level 2)
    paragraph "Each wraps the matching . They take a DOM root (e.g. , )."
      link "assert* function"
        code "assert*"
      code "container"
      code "document.body"
    table
      rowgroup
        row
          rowheader "Matcher"
          rowheader "Asserts"
      rowgroup
        row
          cell "toHaveNoUnlabeledInteractive()"
            code "toHaveNoUnlabeledInteractive()"
          cell "Every interactive node has a non-empty accessible name."
        row
          cell "toHaveValidHeadingOrder()"
            code "toHaveValidHeadingOrder()"
          cell "Exactly one <h1> and no skipped heading levels."
            code "<h1>"
        row
          cell "toHaveLabeledDialogs()"
            code "toHaveLabeledDialogs()"
          cell "Every dialog / alertdialog has an accessible name."
            code "dialog"
            code "alertdialog"
        row
          cell "toHaveValidLandmarks()"
            code "toHaveValidLandmarks()"
          cell "Exactly one main; at most one banner / contentinfo."
            code "main"
            code "banner"
            code "contentinfo"
    button "Copy Code"
      code
    paragraph "On failure the matcher surfaces the underlying assertion's message:"
    button "Copy Code"
      code
    heading "toBeValidA11yTree()" (level 2)
      code "toBeValidA11yTree()"
    paragraph "Asserts the extracted accessibility tree has no ARIA — invalid roles, missing required accessible names, and relationship violations (interactive nesting, presentational-children misuse). Backed by . Advisory don't fail it — only errors do."
      strong "errors"
      code "@real-a11y-dev/validate"
      strong "warnings"
    button "Copy Code"
      code
    paragraph "Unlike the four matchers above, this one doesn't wrap an function — it runs the semantic tree through and fails only on issues."
      code "assert*"
      code "@real-a11y-dev/validate"
      code "severity: "error""
    heading "toHaveTabSequence(expected)" (level 2)
      code "toHaveTabSequence(expected)"
    paragraph "Asserts the computed Tab order equals an array of tokens, in the order a user would encounter pressing Tab (positive first)."
      code "role "name""
      code "tabindex"
    button "Copy Code"
      code
    paragraph "This is the assertion form of . It's especially useful for checks — when a modal is open, the sequence collapses to just the dialog's controls:"
      link "tabSequenceSnapshot"
        code "tabSequenceSnapshot"
      strong "focus-trap"
    button "Copy Code"
      code
    heading "a11ySnapshot(root, options?) — snapshot serializer" (level 2)
      code "a11ySnapshot(root, options?)"
    paragraph "Wrap a DOM root (or a pre-extracted tree) so / render the instead of a DOM dump — fully native to each framework's snapshot tooling (/, obsolete detection all work)."
      code "toMatchSnapshot()"
      code "toMatchInlineSnapshot()"
      strong "semantic tree"
      code "-u"
      code "--update"
    button "Copy Code"
      code
    button "Copy Code"
      code
    paragraph "It accepts the same options as — including to mask dynamic text (timestamps, IDs, prices) so the snapshot stays stable in CI:"
      code "auditSnapshot"
      link "redact"
        code "redact"
    button "Copy Code"
      code
    paragraph "See for the full pattern reference and the -flag gotcha."
      link "redact"
        code "redact"
      code "g"
    paragraph "registers the serializer for you. To register it on its own (e.g. via a framework's config), it's exported as ."
      code "registerA11yMatchers"
      code "snapshotSerializers"
      code "a11ySnapshotSerializer"
    heading "a11ySnapshot vs auditSnapshot" (level 3)
      code "a11ySnapshot"
      code "auditSnapshot"
    paragraph "Both render the — under the hood they call the same serializer with the same options (, , ). The only difference is what they hand back, and therefore how the framework treats it:"
      strong "same tree"
      code "mode"
      code "redact"
      code "includeGeneric"
    list
      listitem "returns a . The value your test holds the tree, so you can assert on it directly — , — log it, or write it to a file. It needs no setup."
        link "auditSnapshot()"
          code "auditSnapshot()"
        strong "plain string"
        emphasis "is"
        code "expect(s).toContain('button "Save"')"
        code "expect(s1).toBe(s2)"
      listitem "returns an that the registered serializer renders at snapshot time. It does nothing on its own, but it keeps / fully native — and crucially, : the tree is rendered as-is instead of escaped into a quoted string literal."
        code "a11ySnapshot()"
        strong "opaque boxed value"
        code "toMatchSnapshot()"
        code "toMatchInlineSnapshot()"
        strong "inline snapshots stay readable"
    table
      rowgroup
        row
          rowheader "Reach for auditSnapshot when…"
            code "auditSnapshot"
          rowheader "Reach for a11ySnapshot when…"
            code "a11ySnapshot"
      rowgroup
        row
          cell "You want the string itself — substring assertions, comparing two trees, writing a CI artifact"
          cell "You're committing the tree with toMatchSnapshot() / toMatchInlineSnapshot()"
            code "toMatchSnapshot()"
            code "toMatchInlineSnapshot()"
        row
          cell "You don't want to register a serializer"
          cell "You want clean, unquoted output — especially for inline snapshots"
            strong "inline"
    paragraph "When in doubt: for "I need the string," for "I'm storing a snapshot of the tree.""
      code "auditSnapshot"
      code "a11ySnapshot"
    heading "Modal scoping in snapshots" (level 3)
    paragraph "Because the snapshot reflects the extracted a11y tree, an open dialog the snapshot — content behind a modal is inert to assistive tech, so it drops out and the snapshot captures only the dialog. That makes the snapshot a precise regression artifact for the modal state, not just the page."
      strong "scopes"
    heading "Vitest vs Jest type augmentation" (level 2)
    paragraph "The matcher signatures are identical; only the type wiring differs:"
    table
      rowgroup
        row
          rowheader "Aspect"
          rowheader "Vitest"
          rowheader "Jest"
      rowgroup
        row
          cell "Runtime registration"
          cell "registerA11yMatchers(expect)"
            code "registerA11yMatchers(expect)"
          cell "registerA11yMatchers(expect)"
            code "registerA11yMatchers(expect)"
        row
          cell "Type augmentation"
          cell "import "@real-a11y-dev/testing/matchers/vitest""
            code "import "@real-a11y-dev/testing/matchers/vitest""
          cell "none — global jest.Matchers augmentation ships with the import"
            code "jest.Matchers"
    heading "See it running" (level 2)
    list
      listitem "— (simple) and (a full a11y gate + -driven modal)"
        link "examples/testing-vitest"
          code "examples/testing-vitest"
        code "matchers-basic.test.ts"
        code "matchers-account-page.test.ts"
        code "flow()"
      listitem "— the minimal Jest + ts-jest setup"
        link "examples/testing-jest"
          code "examples/testing-jest"
    heading "See also" (level 2)
    list
      listitem "and — the functions these matchers wrap"
        link "Assertions"
        link "Snapshots"
      listitem "— the concept and why it complements rule-based audits"
        link "Accessibility Snapshots"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous pageAssertions"
      link "Next pageFlow API"