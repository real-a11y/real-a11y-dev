  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
      link "@real-a11y-dev/testing"
        paragraph "@real-a11y-dev/testing"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "Install"
      listitem
        link "Snapshots"
      listitem
        link "Assertions"
      listitem
        link "Flow API"
      listitem
        link "Utilities"
      listitem
        link "Playwright adapter"
      listitem
        link "TypeScript"
      listitem
        link "See it running"
  main
    heading "@real-a11y-dev/testing ​" (level 1)
      link "Permalink to "@real-a11y-dev/testing""
    blockquote
      paragraph "— Snapshot helpers (, , ) and structural assertions (, , , ) plus a fluent  chain for interaction tests. Works in jsdom (Vitest / Jest) out of the box; add  for real-browser E2E. Reach for this  — unit and e2e alike."
        strong "TL;DR"
        code "auditSnapshot"
        code "outlineSnapshot"
        code "tabSequenceSnapshot"
        code "assertHeadingOrder"
        code "assertNoUnlabeledInteractive"
        code "assertDialogsLabeled"
        code "assertLandmarkStructure"
        code "flow()"
        code "@real-a11y-dev/testing/playwright"
        strong "in your test suite"
    paragraph "Headless accessibility audit helpers for Vitest, Jest, and Playwright. No browser required for the core helpers — they work in jsdom."
    heading "Install ​" (level 2)
      link "Permalink to "Install""
    button "Copy Code"
      code
    separator
    heading "Snapshots ​" (level 2)
      link "Permalink to "Snapshots""
    paragraph "Deterministic string representations of the accessibility tree. Safe to commit — same DOM always produces the same string."
    heading "auditSnapshot(root, options?) ​" (level 3)
      code "auditSnapshot(root, options?)"
      link "Permalink to "`auditSnapshot(root, options?)`""
    paragraph "Returns a formatted string of the full semantic tree."
    button "Copy Code"
      code
    paragraph
      strong "Options:"
    table
      rowgroup
        row
          rowheader "Option"
          rowheader "Type"
          rowheader "Default"
          rowheader "Description"
      rowgroup
        row
          cell "mode"
            code "mode"
          cell ""a11y" | "dom""
            code ""a11y" | "dom""
          cell ""a11y""
            code ""a11y""
          cell "Tree extraction mode."
        row
          cell "redact"
            code "redact"
          cell "RegExp[]"
            code "RegExp[]"
          cell "[]"
            code "[]"
          cell "Patterns replaced with [redacted] in names and text. Use this to keep snapshots deterministic."
            code "[redacted]"
    paragraph "Example output:"
    button "Copy Code"
      code
    heading "Using redact — realistic patterns ​" (level 4)
      code "redact"
      link "Permalink to "Using `redact` — realistic patterns""
    paragraph "Most snapshot flakes come from text that changes between runs. Pass regex patterns that match the noisy bits:"
    button "Copy Code"
      code
    paragraph "The redaction happens  name computation — so an  becomes  in the snapshot string without changing the underlying tree. The element still appears, its role and structure still matter."
      strong "after"
      code "aria-label="Updated 2 minutes ago""
      code "[redacted]"
    paragraph "Redact patterns are additive across calls — define one shared list for your suite:"
    button "Copy Code"
      code
    heading "outlineSnapshot(root) ​" (level 3)
      code "outlineSnapshot(root)"
      link "Permalink to "`outlineSnapshot(root)`""
    paragraph "Returns a string of the heading outline only — useful for structure audits."
    button "Copy Code"
      code
    paragraph "Example output:"
    button "Copy Code"
      code
    heading "tabSequenceSnapshot(root) ​" (level 3)
      code "tabSequenceSnapshot(root)"
      link "Permalink to "`tabSequenceSnapshot(root)`""
    paragraph "Returns the tab sequence as a numbered list."
    button "Copy Code"
      code
    paragraph "Example output:"
    button "Copy Code"
      code
    separator
    heading "Assertions ​" (level 2)
      link "Permalink to "Assertions""
    paragraph "These throw  with a descriptive message on failure. Designed for use directly in  /  blocks without wrapping in ."
      code "A11yAssertionError"
      code "it()"
      code "test()"
      code "expect()"
    heading "assertNoUnlabeledInteractive(root) ​" (level 3)
      code "assertNoUnlabeledInteractive(root)"
      link "Permalink to "`assertNoUnlabeledInteractive(root)`""
    paragraph "Throws if any interactive element (button, link, textbox, combobox, checkbox, radio, etc.) has an empty accessible name."
    button "Copy Code"
      code
    paragraph "Error message example:"
    button "Copy Code"
      code
    heading "assertHeadingOrder(root) ​" (level 3)
      code "assertHeadingOrder(root)"
      link "Permalink to "`assertHeadingOrder(root)`""
    paragraph "Throws if:"
    list
      listitem "There is not exactly one"
        code "h1"
      listitem "Any heading level is skipped (e.g.,  followed directly by )"
        code "h2"
        code "h4"
    button "Copy Code"
      code
    heading "assertDialogsLabeled(root) ​" (level 3)
      code "assertDialogsLabeled(root)"
      link "Permalink to "`assertDialogsLabeled(root)`""
    paragraph "Throws if any  or  element has no accessible name (via  or )."
      code "dialog"
      code "alertdialog"
      code "aria-label"
      code "aria-labelledby"
    button "Copy Code"
      code
    heading "assertLandmarkStructure(root) ​" (level 3)
      code "assertLandmarkStructure(root)"
      link "Permalink to "`assertLandmarkStructure(root)`""
    paragraph "Throws if:"
    list
      listitem "There is no  landmark"
        code "main"
      listitem "There is more than one  landmark"
        code "main"
      listitem "There is more than one  (header) landmark"
        code "banner"
      listitem "There is more than one  (footer) landmark"
        code "contentinfo"
    button "Copy Code"
      code
    separator
    heading "Flow API ​" (level 2)
      link "Permalink to "Flow API""
    paragraph "Fluent interaction chains that assert about the  after each step. Each  /  / etc. dispatches a real DOM action, then re-extracts the tree so the next step sees the post-interaction state."
      strong "accessibility tree"
      code ".click()"
      code ".type()"
    button "Copy Code"
      code
    heading "Why flow() (vs Testing Library)? ​" (level 3)
      code "flow()"
      link "Permalink to "Why `flow()` (vs Testing Library)?""
    paragraph "Testing Library asserts about the .  asserts about the  — the same tree screen-reader users perceive and the same tree the Real A11y panel shows. That difference changes which tests are easy to write."
      strong "rendered DOM"
      code "flow()"
      strong "extracted accessibility tree"
    paragraph "is  a Testing Library replacement. They compose well:  for input-fidelity (real keyboard/pointer sequences);  for tree-shape and "is the right region active" assertions."
      code "flow()"
      strong "not"
      code "userEvent"
      code "flow()"
    heading "Side-by-side: same test, both libraries ​" (level 4)
      link "Permalink to "Side-by-side: same test, both libraries""
    paragraph "A common flow — open a confirm dialog, dismiss it, assert it closed:"
    button "Copy Code"
      code
    button "Copy Code"
      code
    paragraph "Both pass on the same component. The Flow version is shorter because  is a single tree-level invariant — "exactly one dialog is open AND its name matches." Expressing the same in RTL stitches  +  +  +  together. And because Flow re-extracts the tree after , the next  already sees the new dialog without an explicit  scope."
      code "expectActiveModal"
      code "getByRole"
      code "within"
      code "toHaveAccessibleName"
      code "queryByRole"
      code ".click()"
      code "findByRole"
      code "within(dialog)"
    heading "When to use which ​" (level 4)
      link "Permalink to "When to use which""
    table
      rowgroup
        row
          rowheader "Reach for Testing Library when…"
          rowheader "Reach for flow() when…"
            code "flow()"
      rowgroup
        row
          cell "You need userEvent keyboard/pointer fidelity (tab(), keyboard("{Enter}"), hover, paste, etc.)"
            code "userEvent"
            code "tab()"
            code "keyboard("{Enter}")"
          cell "The assertion is about the a11y tree shape, not a single element"
        row
          cell "Per-element matchers fit (toHaveValue, toBeChecked, toBeDisabled)"
            code "toHaveValue"
            code "toBeChecked"
            code "toBeDisabled"
          cell "You want to assert "the right modal is now open" or "no modal is open" in one step"
        row
          cell "Your codebase is already deep in RTL idioms and you want to stay consistent"
          cell "The same audit logic must also run in a real browser via the Playwright adapter"
            link "Playwright adapter"
        row
          cell "Testing one component in isolation"
          cell "The test traces a flow that crosses multiple components/regions"
        row
          cell "You care about the simulated input (e.g. testing a custom keyboard handler)"
          cell "You care about the outcome in the a11y tree (states, structure, active modal)"
            strong "outcome"
    paragraph "The two libraries see different things. RTL sees what  experience.  sees what  experience after the same actions. For most teams the right answer is ."
      emphasis "sighted users using a mouse and keyboard"
      code "flow()"
      emphasis "AT users"
      strong "both, in the same suite"
    heading "Available steps ​" (level 3)
      link "Permalink to "Available steps""
    table
      rowgroup
        row
          rowheader "Step"
          rowheader "Description"
      rowgroup
        row
          cell ".findByRole(role, opts?)"
            code ".findByRole(role, opts?)"
          cell "Move the cursor to the first matching node. Throws if not found."
        row
          cell ".click()"
            code ".click()"
          cell "Dispatch a click action on the current node."
        row
          cell ".submit()"
            code ".submit()"
          cell "Dispatch a submit action (form)."
        row
          cell ".toggle()"
            code ".toggle()"
          cell "Dispatch a toggle action (<details>/<summary>; falls back to click for ARIA disclosures)."
            code "<details>"
            code "<summary>"
        row
          cell ".select(value)"
            code ".select(value)"
          cell "Dispatch a select action with the given value (native <select>)."
            code "<select>"
        row
          cell ".type(text)"
            code ".type(text)"
          cell "Dispatch a type action with the given text (textbox, searchbox)."
        row
          cell ".expectTree(snapshot)"
            code ".expectTree(snapshot)"
          cell "Assert the current tree's serialization matches snapshot (see caveat below)."
            code "snapshot"
        row
          cell ".expectActiveModal(predicate)"
            code ".expectActiveModal(predicate)"
          cell "Assert the active dialog. Pass null to assert no dialog is open, or (name) => boolean to assert one is open and its accessible name satisfies the predicate."
            code "null"
            code "(name) => boolean"
        row
          cell ".expect(fn)"
            code ".expect(fn)"
          cell "Run a custom assertion with the current tree as argument."
    paragraph "The flow is lazy — steps queue up and run when you  the chain."
      code "await"
    heading "expectActiveModal — examples ​" (level 4)
      code "expectActiveModal"
      link "Permalink to "`expectActiveModal` — examples""
    button "Copy Code"
      code
    paragraph "The first  or  in document order is treated as the active modal."
      code "role="dialog""
      code "role="alertdialog""
    heading "expectTree — caveat ​" (level 4)
      code "expectTree"
      link "Permalink to "`expectTree` — caveat""
    paragraph "re-serializes the tree with  (no , , generic nodes flattened). A snapshot captured via  or  will not match. For redacted or DOM-mode comparisons, use  and call / yourself."
      code "expectTree"
      strong "default options"
      code "redact"
      code "mode: "a11y""
      code "auditSnapshot(root, { redact: [...] })"
      code "{ mode: "dom" }"
      code ".expect((tree) => { … })"
      code "serializeTree"
      code "auditSnapshot"
    heading "flow(root, options?) ​" (level 3)
      code "flow(root, options?)"
      link "Permalink to "`flow(root, options?)`""
    table
      rowgroup
        row
          rowheader "Option"
          rowheader "Type"
          rowheader "Default"
          rowheader "Description"
      rowgroup
        row
          cell "waitTimeout"
            code "waitTimeout"
          cell "number"
            code "number"
          cell "200 ms"
            code "200"
          cell "Max wait for the post-action debounced mutation cycle. Increase for slow async UIs; decrease for tighter feedback in pure-DOM flows."
    button "Copy Code"
      code
    separator
    heading "Utilities ​" (level 2)
      link "Permalink to "Utilities""
    heading "waitForMutations(root, options?) ​" (level 3)
      code "waitForMutations(root, options?)"
      link "Permalink to "`waitForMutations(root, options?)`""
    paragraph "Resolves after the next debounced DOM mutation cycle. Useful after programmatic DOM changes."
    button "Copy Code"
      code
    paragraph
      strong "Options:"
    table
      rowgroup
        row
          rowheader "Option"
          rowheader "Type"
          rowheader "Default"
      rowgroup
        row
          cell "timeout"
            code "timeout"
          cell "number"
            code "number"
          cell "1000 ms"
            code "1000"
        row
          cell "debounceMs"
            code "debounceMs"
          cell "number"
            code "number"
          cell "50 ms"
            code "50"
    heading "dispatch(node, action?, payload?) ​" (level 3)
      code "dispatch(node, action?, payload?)"
      link "Permalink to "`dispatch(node, action?, payload?)`""
    paragraph "Dispatches an action on a  directly."
      code "SemanticNode"
    button "Copy Code"
      code
    separator
    heading "Playwright adapter ​" (level 2)
      link "Permalink to "Playwright adapter""
    paragraph "Run the same audit helpers against a real browser page. The adapter injects a self-contained IIFE bundle into the page and exposes every helper via ."
      code "page.evaluate()"
    heading "Install ​" (level 3)
      link "Permalink to "Install""
    button "Copy Code"
      code
    paragraph "The  peer dependency is optional — only needed when using ."
      code "playwright"
      code "@real-a11y-dev/testing/playwright"
    heading "attach(page, options?) ​" (level 3)
      code "attach(page, options?)"
      link "Permalink to "`attach(page, options?)`""
    button "Copy Code"
      code
    paragraph
      strong "options:"
        code "attach()"
    table
      rowgroup
        row
          rowheader "Option"
          rowheader "Type"
          rowheader "Default"
          rowheader "Description"
      rowgroup
        row
          cell "rootSelector"
            code "rootSelector"
          cell "string"
            code "string"
          cell ""body""
            code ""body""
          cell "CSS selector for the audit root element."
    heading "Narrowing the root ​" (level 3)
      link "Permalink to "Narrowing the root""
    button "Copy Code"
      code
    heading "Testing that assertions fail on broken pages ​" (level 3)
      link "Permalink to "Testing that assertions fail on broken pages""
    paragraph "Lock in the failure mode too — not just the happy path. When a new assertion lands or you refactor a page, you want confirmation that broken structure still throws:"
    button "Copy Code"
      code
    paragraph "Keep a small "broken" fixture checked into your test repo with one of each pattern the assertions target (missing h1, skipped level, unlabeled button, unlabeled dialog). If a future refactor ever weakens the assertions, this test catches it immediately."
    heading "Determinism ​" (level 3)
      link "Permalink to "Determinism""
    paragraph ", , and  return the  for the same DOM — on every run, on every machine. No timestamps, no generated IDs, no ordering surprises:"
      code "auditSnapshot()"
      code "outlineSnapshot()"
      code "tabSequenceSnapshot()"
      strong "same string"
    button "Copy Code"
      code
    paragraph "That's the property that makes  safe to use in CI without flakes. If you have genuinely variable content (timestamps, locale-dependent copy), see  above."
      code "expect(await sn.auditSnapshot()).toMatchSnapshot()"
      link "redact"
        code "redact"
    heading "How it works ​" (level 3)
      link "Permalink to "How it works""
    button "Copy Code"
      code
    list
      listitem "reads the pre-built IIFE bundle () from the package."
        code "attach()"
        code "dist/page-bundle.iife.global.js"
      listitem "Injects it into the page via . This sets ."
        code "page.addScriptTag()"
        code "window.__realA11y__"
      listitem "Each method calls , running the helper in the browser and returning the result (or re-throwing the error) in Node."
        code "page.evaluate()"
    paragraph "The bundle is read once and cached in the Node process — subsequent  calls on the same process are instant."
      code "attach()"
    heading "Error propagation ​" (level 3)
      link "Permalink to "Error propagation""
    paragraph "When an assertion throws  inside the page, Playwright propagates the error message back to Node. The full descriptive message (including all offending elements) is preserved."
      code "A11yAssertionError"
    button "Copy Code"
      code
    heading "SemanticNavigatorPageHandle type ​" (level 3)
      code "SemanticNavigatorPageHandle"
      link "Permalink to "`SemanticNavigatorPageHandle` type""
    button "Copy Code"
      code
    paragraph "is a minimal structural type that accepts the real Playwright  object without requiring  or  in packages that only import the types."
      code "PlaywrightPage"
      code "Page"
      code "playwright"
      code "@playwright/test"
    separator
    heading "TypeScript ​" (level 2)
      link "Permalink to "TypeScript""
    button "Copy Code"
      code
    separator
    heading "See it running ​" (level 2)
      link "Permalink to "See it running""
    list
      listitem "— : snapshot tests,  combobox interaction, tab-sequence structure assertions."
        strong "Vitest + jsdom"
        link "examples/testing-vitest/"
          code "examples/testing-vitest/"
        code "flow()"
      listitem "— : a "good fixture" where every assertion passes and a "broken fixture" where each throws — the pattern to keep in CI."
        strong "Playwright E2E"
        link "examples/playwright/"
          code "examples/playwright/"
      listitem "— the  wires a snapshot generator into a PR workflow."
        strong "CI tree-diff bot"
        link "CI Diff Bot recipe"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous page@real-a11y-dev/inspector"
      link "Next page@real-a11y-dev/react"