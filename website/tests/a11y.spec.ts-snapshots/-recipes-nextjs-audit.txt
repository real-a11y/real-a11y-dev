  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Accessibility Snapshots"
        paragraph "Accessibility Snapshots"
      link "Snapshots vs. Other Tools"
        paragraph "Snapshots vs. Other Tools"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "Headings View"
        paragraph "Headings View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
        link "@real-a11y-dev/testing"
          heading "@real-a11y-dev/testing" (level 3)
        button "toggle section"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "Install"
      listitem
        link "Mount <SemanticNavigator /> in a client component"
      listitem
        link "Gate it out of production"
      listitem
        link "Unit tests — Vitest + next-intl + Testing Library"
      listitem
        link "E2E — Playwright against next dev"
      listitem
        link "Known constraints"
  main
    heading "Next.js (App Router + React 19)" (level 1)
      link "Permalink to "Next.js (App Router + React 19)""
    paragraph "Everything on this page assumes Next.js 15 with the App Router and React 19. For legacy Pages Router or React 18, the patterns still work — they're just less load-bearing."
    heading "Install" (level 2)
      link "Permalink to "Install""
    button "Copy Code"
      code
    paragraph "is a dev dependency — you'll gate the panel so it never ships to production. See ."
      code "@real-a11y-dev/react"
      link "Keep it out of production"
    separator
    heading "Mount <SemanticNavigator /> in a client component" (level 2)
      code "<SemanticNavigator />"
      link "Permalink to "Mount `<SemanticNavigator />` in a client component""
    paragraph "Any component that renders the inspector must be a . The inspector uses refs, DOM APIs, and (in floating mode) a portal to  — none of that is available during server rendering."
      strong "Client Component"
      code "document.body"
    button "Copy Code"
      code
    paragraph "The  mode portals into . The library already guards against SSR — the portal only activates after the first client commit — so you can render this component from a Server Component without a crash."
      code "floating"
      code "document.body"
    heading "Gate it out of production" (level 2)
      link "Permalink to "Gate it out of production""
    paragraph "Two options — pick the one that matches your toolchain."
    heading "Option 1 — static gate (fully tree-shaken)" (level 3)
      link "Permalink to "Option 1 — static gate (fully tree-shaken)""
    button "Copy Code"
      code
    paragraph "Note:  has to live  in the App Router. Putting it in a Server Component throws a build-time error."
      code "dynamic(..., { ssr: false })"
      strong "inside a Client Component"
    heading "Option 2 — environment flag" (level 3)
      link "Permalink to "Option 2 — environment flag""
    paragraph "For staging deployments where you want to toggle the panel with an env var:"
    button "Copy Code"
      code
    paragraph "This ships the panel chunk in the production bundle as a separate chunk — it just doesn't run. Cheaper DX, more bytes."
    separator
    heading "Unit tests — Vitest + next-intl + Testing Library" (level 2)
      link "Permalink to "Unit tests — Vitest + next-intl + Testing Library""
    paragraph "The App Router's  navigation helpers (, , etc.) read from the request-scoped intl context, which doesn't exist in a unit-test renderer. Mock them in :"
      code "next-intl"
      code "Link"
      code "usePathname"
      code "vitest.setup.tsx"
    button "Copy Code"
      code
    paragraph "Wire it into :"
      code "vitest.config.ts"
    button "Copy Code"
      code
    paragraph "Note the file is  — the mock itself renders JSX."
      code ".tsx"
    heading "Test a component that uses Link" (level 3)
      code "Link"
      link "Permalink to "Test a component that uses `Link`""
    button "Copy Code"
      code
    paragraph "Only the  are mocked. Your own components render for real."
      emphasis "Next internals"
    separator
    heading "E2E — Playwright against next dev" (level 2)
      code "next dev"
      link "Permalink to "E2E — Playwright against `next dev`""
    button "Copy Code"
      code
    button "Copy Code"
      code
    paragraph "The same  handle exposes  and  — commit all three as fixtures and let PRs diff against them."
      code "attach(page)"
      code "outlineSnapshot"
      code "tabSequenceSnapshot"
    separator
    heading "Known constraints" (level 2)
      link "Permalink to "Known constraints""
    list
      listitem "The component uses  + . Wrap it in a Client Component ()."
        strong "Server Components can't render ."
          code "<SemanticNavigator />"
        code "useRef"
        code "useEffect"
        code ""use client""
      listitem "In Next 15 this is a build-time error, not a runtime one."
        strong "must live inside a Client Component."
          code "next/dynamic({ ssr: false })"
      listitem "for React 19. Older versions declare  as a peer and fail to install."
        strong "must be ≥ 16.1"
          code "@testing-library/react"
        code "react: ^18"
      listitem "Next 15 declares  as an optional peer — install ≥ that version to avoid ."
        strong "version must match Next's peer range."
          code "@playwright/test"
        code "@playwright/test@^1.51.1"
        code "ERESOLVE"
    paragraph "See the  recipe for the full matrix."
      link "Peer Dependencies"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous page@real-a11y-dev/storybook-addon"
      link "Next pageStorybook 8 + React 19"