  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Accessibility Snapshots"
        paragraph "Accessibility Snapshots"
      link "Snapshots vs. Other Tools"
        paragraph "Snapshots vs. Other Tools"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "Headings View"
        paragraph "Headings View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
        link "@real-a11y-dev/testing"
          heading "@real-a11y-dev/testing" (level 3)
        button "toggle section"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
        link "@real-a11y-dev/mcp"
          heading "@real-a11y-dev/mcp" (level 3)
        button "toggle section"
        link "@real-a11y-dev/cli"
          heading "@real-a11y-dev/cli" (level 3)
        button "toggle section"
      link "Commands & flags"
        paragraph "Commands & flags"
      link "Configuration"
        paragraph "Configuration"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Authenticated Pages"
        paragraph "Authenticated Pages"
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "Top-level keys"
      listitem
        link "defaults keys"
      listitem
        link "Not settable"
      listitem
        link "Legacy shorthands"
      listitem
        link "See also"
  main
    heading "a11y.config.json" (level 1)
      code "a11y.config.json"
    paragraph "Your accessibility policy, committed to your repo — the Jest/ESLint model. names the routes you audit and seeds the flags every command runs with, so a configured repo audits itself with no flags: a bare walks the whole list, each view command inherits your emulation and wait settings, and diffs the same routes run to run."
      code "a11y.config.json"
      code "real-a11y audit"
      code "snapshot"
    paragraph "The file is — no JS config, no — and : an unknown or mistyped key ( for , a bad value like ) is a hard error, never a silent no-op. A silently-ignored gate key could un-gate CI, so the loader refuses to guess. It's repo-controlled and low-trust, hence the guardrails: a 1 MB file cap, a 100-entry cap, and a compile-check on every pattern at load — a bad regex fails on startup, not mid-run."
      strong "strict JSON"
      code "eval"
      strong "fail-closed"
      code ""failon""
      link "failOn"
        code "failOn"
      code "failOn: "sometimes""
      link "urls"
        code "urls"
      link "redact"
        code "redact"
    paragraph "The config is . Without one, every command works from flags and positional URLs alone."
      strong "optional"
    paragraph "Auto-discovered as in the directory you run from — no upward walk in v1, so you inherit only a config in the current directory, never a parent's. points elsewhere; ignores a discovered one. Both flags are accepted on every command."
      strong "Discovery."
      code "./a11y.config.json"
      code "--config <file>"
      code "--no-config"
    paragraph "is . An explicit flag always wins, so a config default is a floor you override per run, never a ceiling you can't escape. (The env layer is for the page list and for 's output — see .)"
      strong "Precedence"
      code "flag > env > config defaults > built-in"
      code "A11Y_PAGES"
      code "A11Y_SNAPSHOT_OUT"
      code "snapshot"
      link "urls"
        code "urls"
    paragraph "A minimal, complete config:"
    button "Copy Code"
      code
    heading "Top-level keys" (level 2)
    paragraph "Three keys: (what to audit), (how to audit it), and (what to strip from output). Everything else is rejected — except four back-compat aliases (the old , plus three top-level shorthands); see ."
      link "urls"
        code "urls"
      link "defaults"
        code "defaults"
      link "redact"
        code "redact"
      code "pages"
      link "Legacy shorthands"
    heading "urls" (level 3)
      code "urls"
    paragraph "— optional · non-empty · max 100 entries"
      strong
        code "urls: (string | { url, name?, rootSelector?, sourcePath? })[]"
    paragraph "The project's audit targets. Each entry is a , or an with a required plus optional fields. A bare string's defaults to the URL itself."
      strong "bare URL string"
      strong "object"
      code "url"
      link "name"
        code "name"
    paragraph "A configured list drives a bare and — run either with no positional and it audits every entry, so you don't re-type routes. Single-view commands ( / / / ) still take exactly one URL. reads two snapshot artifacts, not this list (but it does read )."
      code "urls"
      strong
        code "audit"
      strong
        code "snapshot"
      code "tree"
      code "outline"
      code "tabs"
      code "list"
      code "diff"
      link "defaults"
        code "defaults"
    paragraph "Entry fields:"
    list
      listitem "— the target. Any URL the browser can reach: a public site, a local dev server, staging, or a built file path."
        strong
          code "url"
      listitem "— the diff join key and display label. This is what pairs a route across two snapshots, so keep it stable when a URL changes. Defaults to ."
        strong
          code "name"
        code "url"
      listitem "— scope extraction to a region for this route (per-page ); the semantic tree is taken from the matching element down."
        strong
          code "rootSelector"
        link "root"
          code "root"
      listitem "— repo-relative file the route's findings anchor to in . GitHub code scanning only displays results tied to a file path; without it, results anchor to the config file."
        strong
          code "sourcePath"
        link "SARIF"
    button "Copy Code"
      code
    paragraph "Precedence for the audited list: positional URLs → the env var (JSON ) → this list. Positionals are gated as ; env and config go through the stricter URL gate."
      code "A11Y_PAGES"
      code "[{name,url}]"
      code "urls"
      code "arg"
      code "config"
    heading "defaults" (level 3)
      code "defaults"
    paragraph "— optional · always materialized (possibly empty)"
      strong
        code "defaults: object"
    paragraph "Seeds a value for any flag you don't pass, on every command that declares it — the Jest/ESLint "virtual flag" model. Each key mirrors a CLI flag; the merge injects the value in the flag's raw shape and hands it to the command's own parser, so a config default is validated exactly like a typed flag would be. See for the full table."
      link "defaults keys"
        code "defaults"
    paragraph "Two merge rules keep precedence honest:"
    list
      listitem "A default only fills a flag you left unset — it never overwrites you typed."
        strong "Explicit flag wins."
        code "--device"
      listitem "An emulation default (e.g. ) can't seed , which declares no emulation flags — so a default never trips a parser the equivalent flag couldn't."
        strong "Only reaches commands that accept the flag."
        link "device"
          code "device"
        code "login"
    paragraph "A -only config (no ) is valid — useful when you drive URLs positionally but want shared emulation and gate settings."
      code "defaults"
      link "urls"
        code "urls"
    heading "redact" (level 3)
      code "redact"
    paragraph "— optional"
      strong
        code "redact: string[]"
    paragraph "Extra regex patterns applied to URLs and text before anything is printed or written — for stripping session tokens, signed-URL query params, or PII that would otherwise land in a report or a committed snapshot. Each pattern is -compiled at load, so an invalid pattern fails on startup rather than mid-audit."
      code "new RegExp"
    button "Copy Code"
      code
    heading "defaults keys" (level 2)
      code "defaults"
    paragraph "Every key below maps to one CLI flag. The meta line gives its JSON type, the flag it seeds, the built-in default it overrides, and the commands it reaches — a default only seeds a command that that flag. Throughout, means / / / . Order and types are exactly the loader's allowlist; anything outside it is rejected at load."
      strong "declares"
      strong "the view commands"
      code "tree"
      code "outline"
      code "tabs"
      code "list"
    paragraph "Two path-valued keys — and — resolve , so a committed config is portable no matter which directory a command runs from."
      link "storageState"
        code "storageState"
      link "baseline"
        code "baseline"
      strong "relative to the config file's directory"
    heading "root" (level 3)
      code "root"
    paragraph "— flag · default · applies to , , the view commands,"
      strong
        code "root: string"
      code "--root"
      code "body"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Scope extraction to a CSS selector — audit a single region or component instead of the whole page. For per-route scoping, prefer ."
      link "urls[].rootSelector"
        code "urls[].rootSelector"
    heading "device" (level 3)
      code "device"
    paragraph "— flag · default: unset · applies to , , the view commands,"
      strong
        code "device: string"
      code "--device"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Emulate a Playwright device profile — , — to audit what a screen reader hears on the mobile layout. Suppressed when you pass (which reuses a running browser and can't emulate)."
      code ""iPhone 13""
      code ""Pixel 7""
      code "--cdp"
    button "Copy Code"
      code
    heading "viewport" (level 3)
      code "viewport"
    paragraph "— flag · default: unset · applies to , , the view commands,"
      strong
        code "viewport: string"
      code "--viewport"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Explicit viewport as , e.g. . Suppressed when you pass ."
      code "WIDTHxHEIGHT"
      code ""1280x800""
      code "--cdp"
    heading "waitUntil" (level 3)
      code "waitUntil"
    paragraph "— flag · default · applies to , , the view commands, ,"
      strong
        code "waitUntil: string"
      code "--wait-until"
      code "load"
      code "audit"
      code "inspect"
      code "snapshot"
      code "login"
    paragraph "Navigation settle state before extraction: | | | . Bump to for JS-heavy SPAs whose accessible content lands after first paint."
      code "load"
      code "domcontentloaded"
      code "networkidle"
      code "commit"
      code "networkidle"
    heading "settleMs" (level 3)
      code "settleMs"
    paragraph "— flag · default · max · applies to , , the view commands, ,"
      strong
        code "settleMs: number"
      code "--settle"
      code "0"
      code "30000"
      code "audit"
      code "inspect"
      code "snapshot"
      code "login"
    paragraph "Extra milliseconds to wait after resolves — for content that animates or hydrates in after the load event."
      link "waitUntil"
        code "waitUntil"
    heading "timeoutMs" (level 3)
      code "timeoutMs"
    paragraph "— flag · default · max · applies to , , the view commands, ,"
      strong
        code "timeoutMs: number"
      code "--timeout"
      code "30000"
      code "120000"
      code "audit"
      code "inspect"
      code "snapshot"
      code "login"
    paragraph "Navigation timeout in milliseconds."
    heading "headful" (level 3)
      code "headful"
    paragraph "— flag · default · applies to , , the view commands,"
      strong
        code "headful: boolean"
      code "--headful"
      code "false"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Show the browser window instead of running headless. Suppressed when you pass ."
      code "--cdp"
    heading "storageState" (level 3)
      code "storageState"
    paragraph "— flag · default: unset · · applies to , , the view commands,"
      strong
        code "storageState: string"
      code "--storage-state"
      strong "path (resolves from the config dir)"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Audit as a logged-in user, using a session file saved by . The path resolves relative to the config file, so a committed config stays portable. Suppressed when you pass (CDP reuses the running browser's own session). See ."
      code "real-a11y login"
      code "--cdp"
      link "Authenticated pages"
    button "Copy Code"
      code
    heading "auditOrigins" (level 3)
      code "auditOrigins"
    paragraph "— flag (repeatable) · default: the target's own origin · applies to , , the view commands,"
      strong
        code "auditOrigins: string[]"
      code "--audit-origin"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Extra origins allowed under — origin pinning that stops a redirect from routing extraction to an unintended, cookie-matching origin. Each entry must be a full origin like ."
      link "storageState"
        code "storageState"
      code "https://app.example.com"
    heading "format" (level 3)
      code "format"
    paragraph "— flag · default ( on ) · applies to , , the view commands, ,"
      strong
        code "format: string"
      code "-f, --format"
      code "pretty"
      code "json"
      code "snapshot"
      code "audit"
      code "inspect"
      code "snapshot"
      code "diff"
    paragraph "Output format. , so a global default only has to be valid where it lands: / / view commands take ; takes ; takes . A project-wide seeds cleanly but errors on . Suppressed on when you pass the shorthand."
      strong "Validated per command"
      code "audit"
      code "inspect"
      code "pretty | json"
      code "snapshot"
      code "json | md | sarif | junit | jsonl"
      code "diff"
      code "pretty | json | md"
      code "format: "sarif""
      code "snapshot"
      code "audit"
      code "snapshot"
      code "--md"
    heading "rules" (level 3)
      code "rules"
    paragraph "— flag · default: all five rules · applies to , ,"
      strong
        code "rules: string[]"
      code "--rules"
      code "audit"
      code "inspect"
      code "snapshot"
    paragraph "Restrict the run to a subset of the semantic-tree rules. Valid ids: , , , , — an unknown id is a load error."
      code "no-unlabeled-interactive"
      code "image-alt"
      code "heading-order"
      code "dialog-labeled"
      code "landmark-structure"
    button "Copy Code"
      code
    heading "failOn" (level 3)
      code "failOn"
    paragraph "— flag · default ( on ) · applies to , , ,"
      strong
        code "failOn: "error" | "warning" | "never""
      code "--fail-on"
      code "error"
      code "never"
      code "snapshot"
      code "audit"
      code "inspect"
      code "snapshot"
      code "diff"
    paragraph "The gate threshold — the lowest severity that exits . defaults to (it just writes the artifact); set it there to gate the snapshot itself. View commands are never gates and always exit , so this doesn't reach them."
      code "1"
      code "snapshot"
      code "never"
      code "0"
    heading "annotate" (level 3)
      code "annotate"
    paragraph "— flag (as ) · default · applies to ,"
      strong
        code "annotate: boolean"
      code "--no-annotate"
      code "annotate: false"
      code "true"
      code "audit"
      code "inspect"
    paragraph "GitHub Actions annotations (grouped lines + a job summary), on by default. Only is meaningful — it seeds ; is the built-in and seeds nothing."
      code "::error"
      code "annotate: false"
      code "--no-annotate"
      code "annotate: true"
    heading "includeGeneric" (level 3)
      code "includeGeneric"
    paragraph "— flag · default · applies to , the view commands,"
      strong
        code "includeGeneric: boolean"
      code "--include-generic"
      code "false"
      code "inspect"
      code "snapshot"
    paragraph "Keep generic container nodes (no role, no name) in the serialized tree. Off by default so output shows only what a screen reader announces. Declared on every view command; only tree-rendering output (, , ) actually consumes it."
      code "tree"
      code "inspect"
      code "snapshot"
    heading "baseline" (level 3)
      code "baseline"
    paragraph "— flag · default: unset · · applies to ,"
      strong
        code "baseline: string"
      code "--baseline"
      strong "path (resolves from the config dir)"
      code "snapshot"
      code "diff"
    paragraph "Suppress findings a baseline file has accepted — adopt the gate on a codebase with existing debt without silencing new regressions. Suppressed findings stay in the report (marked ), just out of the count. Path resolves relative to the config file. See ."
      code ""suppressed": true"
      code "--fail-on"
      link "Adopt the gate on existing debt"
    heading "ignoreViewLine" (level 3)
      code "ignoreViewLine"
    paragraph "— flag (repeatable) · default: none · applies to"
      strong
        code "ignoreViewLine: string[]"
      code "--ignore-view-line"
      code "diff"
    paragraph "Drop view lines matching these regexes before diffing — for generated content that changes every build (a "last updated" timestamp, a build hash) and would otherwise read as drift on every page."
    button "Copy Code"
      code
    heading "maxLines" (level 3)
      code "maxLines"
    paragraph "— flag · default: full · applies to"
      strong
        code "maxLines: number"
      code "--max-lines"
      code "diff"
    paragraph "Cap each page's structural diff to lines, then . For CI comments — run once uncapped to a log, once capped to the comment. See ."
      emphasis "n"
      code "… N more"
      link "the CI diff bot guide"
    heading "maxPages" (level 3)
      code "maxPages"
    paragraph "— flag · default: all · applies to"
      strong
        code "maxPages: number"
      code "--max-pages"
      code "diff"
    paragraph "Detail at most changed routes, then list the rest by name. For CI comments."
      emphasis "n"
    heading "explain" (level 3)
      code "explain"
    paragraph "— flag · default · applies to"
      strong
        code "explain: boolean"
      code "--explain"
      code "false"
      code "diff"
    paragraph "Add the plain-language structural summary to a diff ("Heading level changed: h2 → h3", "Keyboard tab stop removed …"). Opt-in because it's an interpretive layer; the neutral default makes no claim the raw diff can't back up."
    heading "Not settable" (level 2)
    paragraph "Five declared flags are deliberately from — no config key can populate them:"
      strong "excluded"
      code "defaults"
    list
      listitem "— per-run I/O and log-verbosity concerns, not project policy. Where output goes and how loud a run is belong to the invocation, not the committed file."
        strong "/ /"
          code "--output"
          code "--quiet"
          code "--verbose"
      listitem "— security-sensitive. unlocks the gate and attaches to a running Chrome; a committed config must not be able to silently widen the trust boundary of every run. They stay explicit, per invocation. (They're also the that suppress conflicting defaults — passing drops the seeded / / / , and drops — so a config default never trips a guard against a flag you actually typed.)"
        strong "/"
          code "--allow-file"
          code "--cdp"
        code "--allow-file"
        code "file:"
        code "--cdp"
        emphasis "triggers"
        code "--cdp"
        link "device"
          code "device"
        link "viewport"
          code "viewport"
        link "headful"
          code "headful"
        link "storageState"
          code "storageState"
        code "--md"
        link "format"
          code "format"
    heading "Legacy shorthands" (level 2)
    paragraph "Four top-level keys are accepted for back-compat — each folds into its canonical home:"
    list
      listitem "— the former name for . Still parsed; wins if both are present."
        strong
          code "pages"
        link "urls"
          code "urls"
        code "urls"
      listitem "/ / at the top level — shorthand for the same keys under ."
        strong
          code "rules"
        strong
          code "failOn"
        strong
          code "device"
        code "defaults"
    paragraph "Prefer the current forms; the shorthands may be removed before 1.0."
    heading "See also" (level 2)
    list
      listitem "— commands, flags, and exit codes."
        link "@real-a11y-dev/cli"
          code "@real-a11y-dev/cli"
      listitem "— baselines."
        link "Adopt the gate on existing debt"
      listitem "— + in a PR."
        link "The CI diff bot"
        code "snapshot"
        code "diff"
      listitem "— and ."
        link "Authenticated pages"
        code "storageState"
        code "--cdp"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous pageCommands & flags"
      link "Next pageNext.js (App Router + React 19)"