  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
      link "@real-a11y-dev/testing"
        paragraph "@real-a11y-dev/testing"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "Search"
      listitem
        link "Role filters"
      listitem
        link "Focus tracking"
      listitem
        link "Scoping"
      listitem
        link "Live region monitoring"
      listitem
        link "Keyboard navigation"
      listitem
        link "Panel interaction vs. host side effects"
      listitem
        link "See also"
  main
    heading "Panel features" (level 1)
      link "Permalink to "Panel features""
    blockquote
      paragraph "These behaviors are  — the , the  embed, the , and the . If a feature exists in one, it exists in all of them. Where the per-package APIs differ, this page calls out which prop / option to reach for."
        strong "shared across every Real A11y surface"
        link "Chrome extension"
        link "@real-a11y-dev/inspector"
          code "@real-a11y-dev/inspector"
        link "React component"
        link "Storybook addon"
    separator
    heading "Search" (level 2)
      link "Permalink to "Search""
    paragraph "A live filter over the tree. Matches on , , , , and . Type "email" → every input, label, and heading whose name or content contains "email" stays visible; everything else collapses. Clearing the field restores the full tree."
      strong "tag name"
      strong "role"
      strong "accessible name"
      strong "attributes"
      strong "visible text"
    paragraph "Works in DOM, A11y, and TAB views."
    table
      rowgroup
        row
          rowheader "Surface"
          rowheader "How it appears"
      rowgroup
        row
          cell "Chrome extension"
          cell "The "Search nodes…" input at the top of the panel toolbar"
        row
          cell "inspector / React <SemanticNavigator />"
            code "inspector"
            code "<SemanticNavigator />"
          cell "Same input, rendered by @real-a11y-dev/semantic-navigator-ui"
            code "@real-a11y-dev/semantic-navigator-ui"
        row
          cell "Storybook addon"
          cell "Same input in the addon panel header"
    separator
    heading "Role filters" (level 2)
      link "Permalink to "Role filters""
    paragraph "Six chip-style toggles that hide every node except the matching role (and the ancestors needed to reach it). Click the same chip again to clear."
    table
      rowgroup
        row
          rowheader "Filter"
          rowheader "What it shows"
          rowheader "What it's for"
      rowgroup
        row
          cell "Headings"
            strong "Headings"
          cell "h1–h6 and role="heading""
            code "h1"
            code "h6"
            code "role="heading""
          cell "Verify the document outline — exactly one h1, no skipped levels"
        row
          cell "Links"
            strong "Links"
          cell "<a href> and role="link""
            code "<a href>"
            code "role="link""
          cell "Audit link text — every link should have a unique, meaningful name"
        row
          cell "Buttons"
            strong "Buttons"
          cell "<button> and role="button""
            code "<button>"
            code "role="button""
          cell "Catch icon-only buttons missing aria-label"
            code "aria-label"
        row
          cell "Forms"
            strong "Forms"
          cell "Every form control with its label"
          cell "Catch unlabeled fields fast"
        row
          cell "Landmarks"
            strong "Landmarks"
          cell "main, banner, contentinfo, nav, region, etc."
            code "main"
            code "banner"
            code "contentinfo"
            code "nav"
            code "region"
          cell "Verify structural landmarks — one main, named navs when there are several"
            code "main"
            code "nav"
        row
          cell "Images"
            strong "Images"
          cell "<img> and role="img" with their alt/aria-label"
            code "<img>"
            code "role="img""
          cell "Empty alts (decorative) stand out from missing alts (a bug)"
    paragraph "— the tab sequence is already a flat projection; filtering it by role would double-filter."
      strong "Disabled in TAB view"
    table
      rowgroup
        row
          rowheader "Surface"
          rowheader "How to enable"
      rowgroup
        row
          cell "Chrome extension"
          cell "The chip row above the tree"
        row
          cell "inspector / React"
            code "inspector"
          cell "Built into the panel UI; nothing to wire up"
        row
          cell "Storybook addon"
          cell "Same chip row in the addon panel"
    separator
    heading "Focus tracking" (level 2)
      link "Permalink to "Focus tracking""
    paragraph "When focus tracking is , the panel and the host page stay in sync :"
      strong "on"
      strong "bidirectionally"
    list
      listitem "— when focus moves on the page (Tab, click on a focusable element), the panel draws the highlight overlay on that element, scrolls the tree to its node, and selects the row.  Implemented by the  via a  listener in its content script; the in-page surfaces don't ship this auto-sync (the host app would have to wire it up itself)."
        strong "Page → panel"
        strong "This is what the "Focus sync" toggle controls."
        link "Chrome extension"
        code "focusin"
      listitem "— selecting a tree node draws the highlight overlay on the corresponding element. The Chrome extension  moves real focus to that element on selection — its DevTools panel lives in a separate window, so it can do that without disrupting panel navigation. For the in-page surfaces (, React ), focus and scroll side effects on the host element are , to avoid stealing focus from the panel itself. See ."
        strong "Panel → page"
        emphasis "also"
        code "inspector"
        code "<SemanticNavigator />"
        strong "opt-in"
        link "Panel interaction vs. host side effects"
    heading "When to leave it on" (level 3)
      link "Permalink to "When to leave it on""
    paragraph "The default. It's the single strongest feature for understanding "what a keyboard user is looking at right now." Tab through a form with the panel open and you'll see the experience."
    heading "When to turn it off" (level 3)
      link "Permalink to "When to turn it off""
    paragraph "Three cases:"
    list
      listitem "— forms with 30+ fields, SPAs that grab focus on every interaction, or pages with aggressive focus-trap libraries. The tree would jump around continuously; turning the sync off keeps it stable on the node you're inspecting."
        strong "Focus-heavy pages"
      listitem "— when you've scoped into a dialog or region and want to prod the page without losing your place."
        strong "Auditing a specific subtree"
      listitem "— with the page blacked out, the overlay is suppressed anyway; turning the sync off avoids unnecessary message traffic."
        strong "Curtain mode"
        emphasis "(extension only)"
    heading "How to verify it's working" (level 3)
      link "Permalink to "How to verify it's working""
    paragraph "Focus the page itself first (click anywhere on the page, not the panel), then press Tab. You should see  a blue rectangle on the focused page element  the corresponding tree node highlight in the panel. If only one happens — or neither — see the ."
      strong "both"
      strong "and"
      link "troubleshooting page"
    table
      rowgroup
        row
          rowheader "Surface"
          rowheader "API"
      rowgroup
        row
          cell "Chrome extension"
          cell ""Focus sync" / "Focus OFF" toggle in the toolbar — controls page → panel sync; panel → page focus is always on (separate window, no conflict)"
        row
          cell "inspector"
            code "inspector"
          cell "createInspector({ root, container, highlightOnHover: true, scrollHostOnSelect: true, focusHostOnActivate: true }) — opt in to overlay / scroll / focus on the host page; all three default to off, see"
            code "createInspector({ root, container, highlightOnHover: true, scrollHostOnSelect: true, focusHostOnActivate: true })"
            link "the rationale"
        row
          cell "React <SemanticNavigator />"
            code "<SemanticNavigator />"
          cell "<SemanticNavigator highlightOnHover scrollHostOnSelect focusHostOnActivate /> — same opt-ins as the inspector; see"
            code "<SemanticNavigator highlightOnHover scrollHostOnSelect focusHostOnActivate />"
            link "the package reference"
        row
          cell "Storybook addon"
          cell "Selection draws an overlay on the story preview by default; the addon hardcodes safe values since the manager iframe is isolated from the preview"
    separator
    heading "Scoping" (level 2)
      link "Permalink to "Scoping""
    paragraph "Narrow the audit root to a subtree. Useful when you want to focus on one form, one dialog, or one widget without the rest of the page in the way."
    paragraph "In the panel, double-click any node to scope into it. The tree re-renders showing only that subtree. A breadcrumb at the top shows the path; click any breadcrumb segment to scope back up, or the  to exit to the full tree."
      code "×"
    paragraph "Scoping affects:"
    list
      listitem "in the tree (only the scoped subtree's descendants)"
        strong "What's visible"
      listitem "(search and role filters operate inside the scope)"
        strong "What's audited"
      listitem "(TAB view only lists focusable elements inside the scope)"
        strong "The tab sequence"
    table
      rowgroup
        row
          rowheader "Surface"
          rowheader "API"
      rowgroup
        row
          cell "Chrome extension"
          cell "Double-click any node to scope; breadcrumb + × button to exit"
            code "×"
        row
          cell "inspector"
            code "inspector"
          cell "inspector.setRoot(element) to scope; inspector.setRoot(originalRoot) to exit"
            code "inspector.setRoot(element)"
            code "inspector.setRoot(originalRoot)"
        row
          cell "React <SemanticNavigator />"
            code "<SemanticNavigator />"
          cell "Update the root prop's ref to point at the scoped element"
            code "root"
        row
          cell "@real-a11y-dev/testing"
            code "@real-a11y-dev/testing"
          cell "attach(page, { rootSelector: "form" }) for Playwright; auditSnapshot(form, …) for jsdom — see"
            code "attach(page, { rootSelector: "form" })"
            code "auditSnapshot(form, …)"
            link "the testing reference"
    heading "Dialog auto-scope" (level 3)
      link "Permalink to "Dialog auto-scope""
    paragraph "When the page opens a  or , the panel automatically scopes into it. A "Dialog: …" indicator appears with the dialog's accessible name. This is what a screen reader user effectively gets — the rest of the page becomes inert and the dialog is the only thing they can navigate. Verifying that auto-scope works correctly (proper labeling, focus trap, escape to close) is one of the highest-value audits the panel enables."
      code "<dialog>"
      code "[role="dialog"]"
    separator
    heading "Live region monitoring" (level 2)
      link "Permalink to "Live region monitoring""
    paragraph "Every  region update,  change, and  on the page appears in the panel's live log in real time, labeled  or . The order, timing, and content are what a screen reader would announce."
      code "aria-live"
      code "role="status""
      code "role="alert""
      emphasis "polite"
      emphasis "assertive"
    paragraph "Use this to verify:"
    list
      listitem "announce on appearance"
        strong "Toast notifications"
      listitem "announce when triggered"
        strong "Form validation errors"
      listitem "announce both their start ("Loading…") and completion ("Loaded.")"
        strong "Loading indicators"
      listitem "announce as you type"
        strong "Search-result counts"
    table
      rowgroup
        row
          rowheader "Surface"
          rowheader "Where the log appears"
      rowgroup
        row
          cell "Chrome extension"
          cell "Bottom of the side panel"
        row
          cell "inspector / React"
            code "inspector"
          cell "Bottom of the panel by default"
        row
          cell "Storybook addon"
          cell "Inside the panel — same position"
    separator
    heading "Keyboard navigation" (level 2)
      link "Permalink to "Keyboard navigation""
    paragraph "The panel itself is fully keyboard-operable."
    table
      rowgroup
        row
          rowheader "Key"
          rowheader "Action"
      rowgroup
        row
          cell "↑ / ↓"
            code "↑"
            code "↓"
          cell "Move between tree rows"
        row
          cell "→"
            code "→"
          cell "Expand the current row"
        row
          cell "←"
            code "←"
          cell "Collapse the current row, or move to its parent"
        row
          cell "Enter"
            code "Enter"
          cell "Activate the current node — clicks links, submits buttons, opens menus"
        row
          cell "Space"
            code "Space"
          cell "Same as Enter for most elements; toggles checkboxes/radios"
        row
          cell "/"
            code "/"
          cell "Focus the search input"
        row
          cell "Home / End"
            code "Home"
            code "End"
          cell "Jump to the first / last visible row"
        row
          cell "Esc"
            code "Esc"
          cell "Clear scope, then clear search, then close the panel"
    paragraph "Implemented in  via  — every package that mounts the tree gets the same keymap."
      code "@real-a11y-dev/semantic-navigator-ui"
      code "useTreeKeyboard"
    separator
    heading "Panel interaction vs. host side effects" (level 2)
      link "Permalink to "Panel interaction vs. host side effects""
    paragraph "A distinction worth pinning down before wiring up an in-page panel for the first time, because the framing "interaction is opt-in" gets it backwards:"
    list
      listitem "Selecting a row, clicking a cross-link chip and jumping to its target, expanding / collapsing, arrow-key tree movement — all of this mutates panel state only. Nothing you do  the panel reaches the real DOM until you ask it to. This is identical across the Chrome extension, the inspector embed, the React component, and the Storybook addon."
        strong "Panel-internal interaction is always on, in every surface."
        emphasis "inside"
      listitem "Calling  or  on the  element in response to a panel action — that's opt-in for the in-page surfaces and on by default for the isolated ones."
        strong "Host-page side effects are what's gated."
        code ".focus()"
        code ".scrollIntoView()"
        emphasis "real"
    table
      rowgroup
        row
          rowheader "Surface"
          rowheader "Where the panel renders"
          rowheader "highlightOnHover / scrollHostOnSelect / focusHostOnActivate"
            code "highlightOnHover"
            code "scrollHostOnSelect"
            code "focusHostOnActivate"
      rowgroup
        row
          cell "Chrome extension"
          cell "DevTools panel (separate window)"
          cell "On by default — no focus/scroll conflict possible"
        row
          cell "Storybook addon"
          cell "Manager iframe (separate document from story preview)"
          cell "Hardcoded to safe values — addon doesn't expose the props"
        row
          cell "@real-a11y-dev/inspector"
            code "@real-a11y-dev/inspector"
          cell "Same document as the audited app"
          cell "Off by default — opt in via config"
            strong "Off by default"
        row
          cell "React <SemanticNavigator />"
            code "<SemanticNavigator />"
          cell "Same document as the audited app"
          cell "Off by default — opt in via props"
            strong "Off by default"
    heading "Why the in-page surfaces default to off" (level 3)
      link "Permalink to "Why the in-page surfaces default to off""
    paragraph "If activating a tree row stole focus to the host element, every Enter / Space on a row would yank focus out of the panel and break tree-pattern keyboard navigation. Same for scroll — every arrow-key step would scroll the page out from under you. The extension and Storybook addon don't have that problem because the panel and the inspected DOM live in different windows / iframes;  on a host element in one document can't pull focus out of a panel in another."
      code ".focus()"
    paragraph "The broader principle, recorded in the changelog when these defaults were established:"
      emphasis "opt-in host-app side effects so audits and test harnesses don't disturb the app under test."
    heading "Opting in for the in-page surfaces" (level 3)
      link "Permalink to "Opting in for the in-page surfaces""
    button "Copy Code"
      code
    button "Copy Code"
      code
    paragraph "is the safest of the three to enable — the overlay just paints over the host page without touching its layout or focus.  and  are the ones that affect the page underneath; turn them on when the panel is set up as an active inspection tool, leave them off when the panel is mounted in production for end users."
      code "highlightOnHover"
      code "scrollHostOnSelect"
      code "focusHostOnActivate"
    separator
    heading "See also" (level 2)
      link "Permalink to "See also""
    list
      listitem "·  ·  — what the tree itself shows in each mode"
        link "Reading the DOM View"
        link "A11y View"
        link "TAB View"
      listitem "— the algorithm behind every accessible name in the tree"
        link "Accessible Names"
      listitem "— where the engine and renderer split lives"
        link "Architecture"
      listitem "— empty audits, flapping snapshots, focus-sync diagnostics"
        link "Troubleshooting"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous pageTAB View"
      link "Next pageOverview & Install"