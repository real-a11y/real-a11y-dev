  link "Skip to content"
  banner
    link "Real A11y"
    button "Search"
    navigation "Main Navigation"
      link "Guide"
      link "Recipes"
      link "Packages"
      button "v0.1 · Beta"
    switch "Switch to dark theme"
    link "github"
  complementary
    navigation "Sidebar Navigation"
      button "Introduction"
        heading "Introduction" (level 2)
      link "Getting Started"
        paragraph "Getting Started"
      link "Core Concepts"
        paragraph "Core Concepts"
      link "Accessibility Snapshots"
        paragraph "Accessibility Snapshots"
      link "Snapshots vs. Other Tools"
        paragraph "Snapshots vs. Other Tools"
      link "Why Real A11y?"
        paragraph "Why Real A11y?"
      button "Reading the Tree"
        heading "Reading the Tree" (level 2)
      link "Understanding the Views"
        paragraph "Understanding the Views"
      link "DOM View"
        paragraph "DOM View"
      link "A11y View"
        paragraph "A11y View"
      link "Headings View"
        paragraph "Headings View"
      link "TAB View"
        paragraph "TAB View"
      link "Panel features"
        paragraph "Panel features"
      button "Chrome Extension"
        heading "Chrome Extension" (level 2)
      link "Overview & Install"
        paragraph "Overview & Install"
      button "Packages"
        heading "Packages" (level 2)
      link "@real-a11y-dev/core"
        paragraph "@real-a11y-dev/core"
      link "@real-a11y-dev/inspector"
        paragraph "@real-a11y-dev/inspector"
        link "@real-a11y-dev/testing"
          heading "@real-a11y-dev/testing" (level 3)
        button "toggle section"
      link "@real-a11y-dev/react"
        paragraph "@real-a11y-dev/react"
      link "@real-a11y-dev/storybook-addon"
        paragraph "@real-a11y-dev/storybook-addon"
        link "@real-a11y-dev/mcp"
          heading "@real-a11y-dev/mcp" (level 3)
        button "toggle section"
      link "Tools reference"
        paragraph "Tools reference"
        link "@real-a11y-dev/cli"
          heading "@real-a11y-dev/cli" (level 3)
        button "toggle section"
      button "Recipes"
        heading "Recipes" (level 2)
      link "Next.js (App Router + React 19)"
        paragraph "Next.js (App Router + React 19)"
      link "Storybook 8 + React 19"
        paragraph "Storybook 8 + React 19"
      link "Peer Dependencies"
        paragraph "Peer Dependencies"
      link "CI Diff Bot"
        paragraph "CI Diff Bot"
      button "Reference"
        heading "Reference" (level 2)
      link "Authenticated Pages"
        paragraph "Authenticated Pages"
      link "Accessible Names"
        paragraph "Accessible Names"
      link "Architecture"
        paragraph "Architecture"
      link "Troubleshooting"
        paragraph "Troubleshooting"
  navigation "On this page"
    heading "On this page" (level 2)
    list
      listitem
        link "Connect it to your client"
      listitem
        link "Your first audit"
      listitem
        link "Auditing a page behind a login"
      listitem
        link "Configuration"
      listitem
        link "Scripting audits without an MCP client"
      listitem
        link "How it compares to Playwright MCP"
      listitem
        link "Limitations"
      listitem
        link "See also"
  main
    heading "@real-a11y-dev/mcp" (level 1)
    paragraph "Beta — preview page"
    paragraph "Published on npm as a : the API and tool surface may still change before 1.0, and audit fidelity is bounded by known engine issues. Pin a version rather than tracking the latest tag if you build on it."
      strong "beta"
    paragraph "The Real A11y MCP server gives an AI assistant a real browser and the accessibility tree behind any page, so it can a URL and report the defects a screen reader would announce — not guess from pasted HTML (which hides computed roles and visibility) or a screenshot (which has no semantics). Point any client at it and ask it to audit a page in plain language."
      strong "audit"
      link "Model Context Protocol"
    paragraph "Accessibility is a property of the page — the roles, names, and visibility a browser actually computes, not what's in the HTML source. So the server drives a real browser (via Playwright) rather than parsing markup. You need:"
      strong "rendered"
    list
      listitem "— the server runs under Node and is fetched with ."
        strong "Node.js 20+"
        code "npx"
      listitem "— install one with ."
        strong "A Chromium binary"
        code "npx playwright install chromium"
      listitem "— Claude Code, Claude Desktop, Cursor, VS Code, Windsurf, or any other MCP-capable tool."
        strong "An MCP client"
    heading "Connect it to your client" (level 2)
    paragraph "The server speaks MCP over stdio — point your client at . No install step is needed; fetches it on first run."
      code "npx -y @real-a11y-dev/mcp"
      code "npx"
    radio "Claude Code"
    radio "VS Code"
    radio "Config file"
    button "Copy Code"
      code
    paragraph "For clients configured by file, add the block above to:"
      code "mcpServers"
    list
      listitem "— (Settings → Developer → Edit Config)."
        strong "Claude Desktop"
        code "claude_desktop_config.json"
      listitem "— , or a project-local ."
        strong "Cursor"
        code "~/.cursor/mcp.json"
        code ".cursor/mcp.json"
      listitem "— that client's MCP config, using the same block."
        strong "Windsurf and others"
    paragraph "Then install a browser once:"
    button "Copy Code"
      code
    paragraph "To pin a version instead of tracking the latest, add it to your project () and point / at the local install."
      code "npm install -D @real-a11y-dev/mcp playwright"
      code "command"
      code "args"
    heading "Your first audit" (level 2)
    paragraph "Once it's connected, ask in plain language — the assistant picks the tools:"
    blockquote
      paragraph ""Audit example.com for accessibility problems.""
        strong "You:"
      paragraph "calls → → gets structured findings → explains them and proposes fixes."
        strong "Assistant:"
        code "open_page("https://example.com")"
        code "audit_page()"
    paragraph "Because it drives a real browser, JS-heavy SPAs render fully, and any URL the browser can reach works — public sites, a , or staging."
      strong "local dev server"
    paragraph "Every tool + parameters →"
      link "/packages/mcp/tools"
    paragraph "This is a guide. For the full tool reference — all ten tools, their parameters, and when to reach for each — see the ."
      link "tools reference"
    heading "Auditing a page behind a login" (level 2)
    paragraph "To audit a page only a logged-in user can see, save a browser session once, then point the server at it — the assistant never touches credentials."
    list
      listitem
        paragraph "with the CLI's login helper (a one-time human step):"
          strong "Save a session"
        button "Copy Code"
          code
        paragraph "A real browser opens; sign in by hand (passwords, SSO, MFA, and passkeys all work), then press Enter. Playwright's storage state — cookies and origin storage — is written to . Keep that file out of version control; it holds live session tokens."
          code "auth.json"
      listitem
        paragraph "with two environment variables in your MCP config:"
          strong "Point the server at it"
        button "Copy Code"
          code
      listitem
        paragraph "Every page now opens already authenticated, and tells the assistant the session is active so it won't try to log in. pins auditing to that origin, so a redirect can't route your session to another site."
          strong "Audit as usual."
          code "open_page"
          code "REAL_A11Y_MCP_ALLOWED_ORIGINS"
    paragraph "The session path is server configuration, never a tool argument, so tokens stay out of the assistant's context. See for the full workflow and security rules."
      link "Authenticated pages"
    heading "Configuration" (level 2)
    paragraph "Set these environment variables on the server process — most clients accept an object alongside / in the config block above."
      code ""env""
      code "command"
      code "args"
    table
      rowgroup
        row
          rowheader "Variable"
          rowheader "Effect"
      rowgroup
        row
          cell "REAL_A11Y_MCP_CDP"
            code "REAL_A11Y_MCP_CDP"
          cell "Attach to an already-running Chrome over CDP (e.g. http://localhost:9222) instead of launching one — the interactive way to audit a login."
            code "http://localhost:9222"
        row
          cell "REAL_A11Y_MCP_HEADFUL"
            code "REAL_A11Y_MCP_HEADFUL"
          cell "1 launches a visible browser instead of headless."
            code "1"
        row
          cell "REAL_A11Y_MCP_ALLOW_FILE"
            code "REAL_A11Y_MCP_ALLOW_FILE"
          cell "1 permits auditing file:// URLs (off by default — an agent that can open file:///…/.env and read the DOM back is a local-file exfiltration risk)."
            code "1"
            code "file://"
            code "file:///…/.env"
        row
          cell "REAL_A11Y_MCP_STORAGE_STATE"
            code "REAL_A11Y_MCP_STORAGE_STATE"
          cell "Path to a saved login session (a Playwright storage-state file) — audit authenticated pages as that session. Never a tool parameter, so session tokens stay out of the agent's context. See Authenticated pages ."
            link "Authenticated pages"
        row
          cell "REAL_A11Y_MCP_ALLOWED_ORIGINS"
            code "REAL_A11Y_MCP_ALLOWED_ORIGINS"
          cell "Comma-separated origins auditing is pinned to when a session is loaded — strongly recommended with STORAGE_STATE so a redirect can't audit an unintended site."
            code "STORAGE_STATE"
    paragraph "When a storage state is configured, tells the agent the session is already active so it won't try to log in — see the guide for the full workflow and security rules."
      code "open_page"
      link "Authenticated pages"
    paragraph "Use a dedicated profile for CDP"
    paragraph "Attaching over CDP connects to whatever Chrome is listening on that port — , which an LLM-driven server can then read. Launch a throwaway instance rather than enabling remote debugging on your main profile:"
      strong "including your everyday browser and its logged-in sessions"
    button "Copy Code"
      code
    heading "Scripting audits without an MCP client" (level 2)
    paragraph "Beyond the server, the package ships a subpath export that gives you the Playwright-backed session standalone — without pulling in the MCP SDK dependency graph:"
      code "./browser"
    button "Copy Code"
      code
    paragraph "(with its option types , , ) drives a real browser, injects the extraction bundle, and returns findings, tree, outline, and tab order from a single extraction — handy for scripting audits directly in Node."
      code "BrowserSession"
      code "BrowserSessionOptions"
      code "OpenOptions"
      code "SnapshotOptions"
    heading "How it compares to Playwright MCP" (level 2)
    paragraph "This a browser-automation MCP such as — it doesn't try to replace it. They solve different halves of the problem:"
      strong "complements"
      link "Playwright MCP"
    list
      listitem "lets an agent a page — click, type, navigate — using the accessibility snapshot as a means to ."
        strong "Playwright MCP"
        emphasis "drive"
        emphasis "act"
      listitem "lets an agent a page — it reports what assistive technology would announce as , with a semantic tree tuned to what a screen reader actually perceives."
        strong "Real A11y"
        emphasis "judge"
        emphasis "broken"
    paragraph "Use Playwright MCP to act; use Real A11y to audit. They pair naturally: drive a flow to a given state with one, then check that state's accessibility with the other."
    paragraph "Two different "Playwrights""
    paragraph "This package is — the browser driver you install as a peer dependency. It , the automation server above. Same underlying engine, different jobs."
      emphasis "built on"
      strong "Playwright the library"
      emphasis "complements"
      strong "Playwright MCP"
    heading "Limitations" (level 2)
    list
      listitem "It runs five rules today — unlabeled interactive elements, images missing alt text, heading order, dialog labeling, and landmark structure — plus the full semantic tree. It is not a complete WCAG or axe-core suite; it is semantic-tree-based and tuned to "what a screen reader announces." For contrast, focus visibility, and other rendered/interactive checks, pair it with and manual testing."
        strong "Scope."
        link "axe-core"
      listitem "The tree is a reimplementation of accessible-name / role computation, not the browser's native accessibility tree. It is tuned to match what assistive tech announces, but can diverge on edge cases."
        strong "Fidelity follows the engine."
      listitem "The assistant's environment must be able to launch Chromium or connect to one over CDP."
        strong "Requires a real browser."
    heading "See also" (level 2)
    list
      listitem "— every tool, its parameters, and when to use it."
        link "Tools reference"
      listitem "— the same engine, for unit and end-to-end tests."
        link "@real-a11y-dev/testing — Assertions"
          code "@real-a11y-dev/testing"
        code "collectFindings"
  contentinfo
    link "Edit this page on GitHub"
    paragraph "Last updated:"
      time "[redacted]"
    navigation "Pager"
      link "Previous page@real-a11y-dev/storybook-addon"
      link "Next pageTools reference"