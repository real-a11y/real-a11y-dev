  h2 Introduction
  h2 Reading the Tree
  h2 Chrome Extension
  h2 Packages
    h3 @real-a11y-dev/testing
  h2 Recipes
  h2 Reference
  h2 On this page
h1 CI A11y Diff Bot
  h2 How it works
  h2 What the comment looks like
    h3 Accessibility diff — 0 new · 0 changed · 0 fixed
    h3 Accessibility diff — 2 new · 0 changed · 1 fixed · structure changed on 2 pages
      h4 Home
  h2 Setup
    h3 1. Install
    h3 2. Describe your pages in a11y.config.json
    h3 3. Add the workflow
  h2 Configuration
    h3 a11y.config.json shape
  h2 Workflow templates
    h3 Next.js (dev server)
    h3 Vite / CRA SPA (preview server)
    h3 Static site (serve build output)
    h3 pnpm monorepo
    h3 Shared: diff & comment job
  h2 Output format
  h2 What it catches
  h2 Combine with assertions