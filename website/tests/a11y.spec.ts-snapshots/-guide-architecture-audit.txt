link "Skip to content"
banner
  link "Real A11y"
  button "Search"
  navigation "Main Navigation"
    link "Guide"
    link "Recipes"
    link "Packages"
    button "v0.1 · Beta"
  switch "Switch to dark theme"
  link "github"
complementary
  navigation "Sidebar Navigation"
    button "Introduction"
      heading "Introduction" (level 2)
    link "Getting Started"
      paragraph "Getting Started"
    link "Core Concepts"
      paragraph "Core Concepts"
    link "Accessibility Snapshots"
      paragraph "Accessibility Snapshots"
    link "Snapshots vs. Other Tools"
      paragraph "Snapshots vs. Other Tools"
    link "Why Real A11y?"
      paragraph "Why Real A11y?"
    button "Reading the Tree"
      heading "Reading the Tree" (level 2)
    link "Understanding the Views"
      paragraph "Understanding the Views"
    link "DOM View"
      paragraph "DOM View"
    link "A11y View"
      paragraph "A11y View"
    link "Headings View"
      paragraph "Headings View"
    link "TAB View"
      paragraph "TAB View"
    link "Panel features"
      paragraph "Panel features"
    button "Chrome Extension"
      heading "Chrome Extension" (level 2)
    link "Overview & Install"
      paragraph "Overview & Install"
    button "Packages"
      heading "Packages" (level 2)
    link "@real-a11y-dev/inspector"
      paragraph "@real-a11y-dev/inspector"
    link "@real-a11y-dev/testing"
      heading "@real-a11y-dev/testing" (level 3)
    button "toggle section"
    link "@real-a11y-dev/react"
      paragraph "@real-a11y-dev/react"
    link "@real-a11y-dev/storybook-addon"
      paragraph "@real-a11y-dev/storybook-addon"
    link "@real-a11y-dev/mcp"
      heading "@real-a11y-dev/mcp" (level 3)
    button "toggle section"
    link "@real-a11y-dev/cli"
      heading "@real-a11y-dev/cli" (level 3)
    button "toggle section"
    button "Recipes"
      heading "Recipes" (level 2)
    link "Next.js (App Router + React 19)"
      paragraph "Next.js (App Router + React 19)"
    link "Storybook 8 + React 19"
      paragraph "Storybook 8 + React 19"
    link "Peer Dependencies"
      paragraph "Peer Dependencies"
    link "CI Diff Bot"
      paragraph "CI Diff Bot"
    button "Reference"
      heading "Reference" (level 2)
    link "Authenticated Pages"
      paragraph "Authenticated Pages"
    link "Accessible Names"
      paragraph "Accessible Names"
    link "Architecture"
      paragraph "Architecture"
    link "Troubleshooting"
      paragraph "Troubleshooting"
navigation "On this page"
  heading "On this page" (level 2)
  list
    listitem
      link "Packages"
    listitem
      link "Dependency graph"
    listitem
      link "Why this split"
    listitem
      link "Build pipeline per package"
    listitem
      link "The SemanticNode data model"
    listitem
      link "Where to read next"
main
  heading "Architecture" (level 1)
  paragraph "Real A11y is a monorepo of small, composable packages built around one extraction engine. This page explains what each package owns, how they depend on each other, and why the split looks the way it does."
  separator
  heading "Packages" (level 2)
  heading "Published to npm" (level 3)
  table
    rowgroup
      row
        columnheader "Package"
        columnheader "Purpose"
        columnheader "Runtime deps"
    rowgroup
      row
        cell "@real-a11y-dev/inspector"
          link "@real-a11y-dev/inspector"
            code "@real-a11y-dev/inspector"
        cell "Framework-agnostic inspector. createInspector({ root, container }) mounts the tree panel into any DOM node, isolated via Shadow DOM."
          code "createInspector({ root, container })"
        cell "preact (bundles the internal core and semantic-navigator-ui)"
          code "preact"
          code "core"
          code "semantic-navigator-ui"
      row
        cell "@real-a11y-dev/react"
          link "@real-a11y-dev/react"
            code "@real-a11y-dev/react"
        cell "React integration — <SemanticNavigator /> component + useSemanticTree() / useActiveModal() hooks. Wraps inspector for inline and floating modes."
          code "<SemanticNavigator />"
          code "useSemanticTree()"
          code "useActiveModal()"
          code "inspector"
        cell "react >= 18, react-dom >= 18 (bundles the internal core)"
          code "react >= 18"
          code "react-dom >= 18"
          code "core"
      row
        cell "@real-a11y-dev/testing"
          link "@real-a11y-dev/testing"
            code "@real-a11y-dev/testing"
        cell "Headless audit helpers — auditSnapshot, outlineSnapshot, tabSequenceSnapshot, flow(), plus the interaction-diff API (capture, a11yDiff). Re-exports the assert*/collectFindings surface from the internal audit and the serializers from the internal serialize — with both private, this is where that vocabulary is published. A separate /playwright entrypoint ships a Page-handle adapter that injects the internal browser driver's page-bundle for real-browser E2E. No UI."
          code "auditSnapshot"
          code "outlineSnapshot"
          code "tabSequenceSnapshot"
          code "flow()"
          code "capture"
          code "a11yDiff"
          code "assert*"
          code "collectFindings"
          code "audit"
          code "serialize"
          code "/playwright"
          code "Page"
          code "browser"
          strong "No UI."
        cell "None (bundles the internal core, browser, audit, serialize, and validate; optional: @playwright/test)"
          code "core"
          code "browser"
          code "audit"
          code "serialize"
          code "validate"
          code "@playwright/test"
      row
        cell "@real-a11y-dev/storybook-addon"
          link "@real-a11y-dev/storybook-addon"
            code "@real-a11y-dev/storybook-addon"
        cell "Storybook 8 panel — preview-side extractor posts tree snapshots over the Storybook channel; manager-side React panel renders them."
        cell "storybook >= 8, react >= 18 (every entry bundles the internal core; the manager entry also bundles semantic-navigator-ui)"
          code "storybook >= 8"
          code "react >= 18"
          code "core"
          code "semantic-navigator-ui"
      row
        cell "@real-a11y-dev/mcp"
          link "@real-a11y-dev/mcp"
            code "@real-a11y-dev/mcp"
        cell "Model Context Protocol server exposing audit_page / get_semantic_tree / inspect_page, plus a11y snapshot checkpoints (checkpoint_findings / diff_findings / …), to AI agents (bin real-a11y-mcp). Also the published home for BrowserSession and its session types, which an embedder needs in order to hand buildServer a session of its own."
          code "audit_page"
          code "get_semantic_tree"
          code "inspect_page"
          strong "a11y snapshot checkpoints"
          code "checkpoint_findings"
          code "diff_findings"
          code "real-a11y-mcp"
          code "BrowserSession"
          code "buildServer"
        cell "@modelcontextprotocol/sdk (bundles the internal core, browser, audit, serialize, snapshot, and session-registry; optional peer: playwright)"
          code "@modelcontextprotocol/sdk"
          code "core"
          code "browser"
          code "audit"
          code "serialize"
          code "snapshot"
          code "session-registry"
          code "playwright"
      row
        cell "@real-a11y-dev/cli"
          link "@real-a11y-dev/cli"
            code "@real-a11y-dev/cli"
        cell "The real-a11y shell command — audits, perception views (tree / outline / tabs / list / inspect), and snapshot + diff from the shell and CI. A command, not a library — and with snapshot internal there is no library to import, so --format json and -o on snapshot / diff are the programmatic surface."
          code "real-a11y"
          code "tree"
          code "outline"
          code "tabs"
          code "list"
          code "inspect"
          code "snapshot"
          code "diff"
          code "snapshot"
          code "--format json"
          code "-o"
          code "snapshot"
          code "diff"
        cell "@puppeteer/browsers (bundles the internal core, browser, audit, serialize, snapshot, and session-registry; optional peer: playwright)"
          code "@puppeteer/browsers"
          code "core"
          code "browser"
          code "audit"
          code "serialize"
          code "snapshot"
          code "session-registry"
          code "playwright"
  heading "Internal — bundled, not published" (level 3)
  paragraph "These live in the same repo and their code still ships; it ships the packages above. tsup's inlines the JS and inlines the declarations, so no published names them either — there is nothing to install and no version to pin. The line isn't drawn by the build: a package is on npm because someone installs it on purpose, so a seam that exists to keep the source tidy belongs here. The seven are not all the same kind of seam, though. and were never named by a published declaration; 's and 's vocabulary was, so re-exports it rather than leaving it stranded — that is its only published home. is the exception — nothing re-exports it, and running the CLI or the MCP tools is the only way to reach it. sits between the two: re-exports the five names its embedding contract needs ( and its session types) and nothing else, so the driver itself is something you run rather than install. is the largest of them and splits both ways at once — a fifth of its names stay published because the packages above name them, and the extractors at the bottom of it do not."
    emphasis "inside"
    code "noExternal"
    code "dts.resolve"
    code ".d.ts"
    code "ui"
    code "validate"
    code "serialize"
    code "audit"
    code "testing"
    code "snapshot"
    code "browser"
    code "mcp"
    code "BrowserSession"
    code "core"
  table
    rowgroup
      row
        columnheader "Package"
        columnheader "Purpose"
        columnheader "Reaches you through"
    rowgroup
      row
        cell "@real-a11y-dev/core"
          link "@real-a11y-dev/core"
            code "@real-a11y-dev/core"
        cell "Extraction engine — accessibility + DOM tree walk, role map, accessible-name computation, action dispatch, DOM observer, stable-id generator, tree queries. No UI. Published up to 0.1.0-beta.13."
          strong "No UI."
          code "0.1.0-beta.13"
        cell "Every published package bundles it. testing re-exports the query and diff vocabulary — findByRole, findAllByRole, diffTrees, getOutline, getTabSequence, linearize, with SemanticNode, ExtractionResult, TreeDiff, NodeChange, OutlineEntry, RoleFilter and FindByRoleOptions; react and inspector re-export the node, action and config types their own APIs name. The extractors themselves (extractA11yTree, extractDomTree) and the live machinery (LiveTreeExtractor, DomObserver, FocusManager, ActionDispatcher, createPicker) have no published home — they are reached by running a surface"
          code "testing"
          code "findByRole"
          code "findAllByRole"
          code "diffTrees"
          code "getOutline"
          code "getTabSequence"
          code "linearize"
          code "SemanticNode"
          code "ExtractionResult"
          code "TreeDiff"
          code "NodeChange"
          code "OutlineEntry"
          code "RoleFilter"
          code "FindByRoleOptions"
          code "react"
          code "inspector"
          code "extractA11yTree"
          code "extractDomTree"
          code "LiveTreeExtractor"
          code "DomObserver"
          code "FocusManager"
          code "ActionDispatcher"
          code "createPicker"
      row
        cell "@real-a11y-dev/serialize"
          link "@real-a11y-dev/serialize"
            code "@real-a11y-dev/serialize"
        cell "Deterministic text serialization of the tree — full tree, heading outline, and tab sequence. No UI. Published up to 0.1.0-beta.12."
          strong "No UI."
          code "0.1.0-beta.12"
        cell "testing, which re-exports extract, SerializeOptions, and the auditSnapshot / outlineSnapshot / tabSequenceSnapshot serializers; also inlined into cli, mcp, and the extension"
          code "testing"
          code "extract"
          code "SerializeOptions"
          code "auditSnapshot"
          code "outlineSnapshot"
          code "tabSequenceSnapshot"
          code "cli"
          code "mcp"
      row
        cell "@real-a11y-dev/audit"
          link "@real-a11y-dev/audit"
            code "@real-a11y-dev/audit"
        cell "Audit engine — the Finding data model, the a11y rule set, collectFindings, and the assert* primitives. The one place a finding is defined and detected. No UI. Published up to 0.1.0-beta.12."
          code "Finding"
          code "collectFindings"
          code "assert*"
          strong "No UI."
          code "0.1.0-beta.12"
        cell "testing, which re-exports Finding, A11yRule, ALL_RULES, collectFindings and the assert* primitives, and mcp, which re-exports Finding; also inlined into cli"
          code "testing"
          code "Finding"
          code "A11yRule"
          code "ALL_RULES"
          code "collectFindings"
          code "assert*"
          code "mcp"
          code "Finding"
          code "cli"
      row
        cell "@real-a11y-dev/snapshot"
          link "@real-a11y-dev/snapshot"
            code "@real-a11y-dev/snapshot"
        cell "Snapshot engine — deterministic finding fingerprints, the diffable a11y-snapshot.json artifact, the findings/views/unified diff, and baselines. Node-only; the single place a snapshot is captured and compared, so the CLI and MCP diff identically. No UI. Published up to 0.1.0-beta.12."
          code "a11y-snapshot.json"
          strong "No UI."
          code "0.1.0-beta.12"
        cell "Nothing re-exports it. cli (real-a11y snapshot / real-a11y diff, both take --format json and -o) and mcp's checkpoint tools are the only way to run it"
          code "cli"
          code "real-a11y snapshot"
          code "real-a11y diff"
          code "--format json"
          code "-o"
          code "mcp"
      row
        cell "@real-a11y-dev/browser"
          link "@real-a11y-dev/browser"
            code "@real-a11y-dev/browser"
        cell "Browser driver — the Playwright BrowserSession plus the injected page-bundle it ships. The one place that touches Playwright; the CLI, the MCP server, and the testing adapter all drive a real Chromium through it. Published up to 0.1.0-beta.13."
          code "BrowserSession"
          code "0.1.0-beta.13"
        cell "cli and mcp by running them, and testing's Playwright adapter, which injects its page-bundle. mcp also re-exports BrowserSession, A11ySession, BrowserSessionOptions, PageSnapshot and SnapshotOptions for the SessionManager contract; the rest of the surface (nativeTree, resolveTarget, pageBundleSource, the Chrome-resolution helpers) has no published home"
          code "cli"
          code "mcp"
          code "testing"
          code "mcp"
          code "BrowserSession"
          code "A11ySession"
          code "BrowserSessionOptions"
          code "PageSnapshot"
          code "SnapshotOptions"
          code "SessionManager"
          code "nativeTree"
          code "resolveTarget"
          code "pageBundleSource"
      row
        cell "@real-a11y-dev/semantic-navigator-ui"
          link "@real-a11y-dev/semantic-navigator-ui"
            code "@real-a11y-dev/semantic-navigator-ui"
        cell "Preact tree-view components — TreePanel, TreeNode, FilteredList, TabSequenceView, theming CSS. Published up to 0.1.0-beta.11."
          code "0.1.0-beta.11"
        cell "inspector (and so react, which depends on it), storybook-addon's manager entry"
          code "inspector"
          code "react"
          code "storybook-addon"
      row
        cell "@real-a11y-dev/validate"
          link "@real-a11y-dev/validate"
            code "@real-a11y-dev/validate"
        cell "ARIA-semantics validation — per-node rules plus tree-level relationship checks, backed by aria-query so it tracks the spec. No internal deps. Published up to 0.1.0-beta.7."
          code "aria-query"
          code "0.1.0-beta.7"
        cell "testing, where the toBeValidA11yTree matcher runs the rules"
          code "testing"
          code "toBeValidA11yTree"
  paragraph "is internal too, but it isn't a library: it builds the Chrome extension from the same engine and ships through the Chrome Web Store, not a registry."
    code "@real-a11y-dev/semantic-navigator-extension"
  separator
  heading "Dependency graph" (level 2)
  button "Copy Code"
  code
  paragraph "Two observations:"
  list
    listitem "Assertions, snapshots, and diffs only read the tree or operate on data; they never render. That's what makes them safe for jsdom and Node and fast enough to run in every unit test."
      strong "and the internal have zero UI dependency."
        code "@real-a11y-dev/testing"
        code "snapshot"
    listitem "and the Storybook addon's manager entry pull through in tsup ( gets it transitively, through ); does the same with , , and , itself with and , and and with , , , and . Consumers only ever install the top-level package; they never reason about Preact versions or tree-view internals, and never resolve a name npm has no copy of."
      strong "Internal packages are bundled into their consumers, not installed alongside them."
      code "inspector"
      code "@real-a11y-dev/semantic-navigator-ui"
      code "noExternal"
      code "react"
      code "inspector"
      code "testing"
      code "browser"
      code "validate"
      code "audit"
      code "serialize"
      code "browser"
      code "audit"
      code "serialize"
      code "cli"
      code "mcp"
      code "browser"
      code "session-registry"
      code "audit"
      code "serialize"
      code "snapshot"
  separator
  heading "Why this split" (level 2)
  heading "Engine separate from renderer" (level 3)
  paragraph "Everything useful about the extraction engine (tree walk, role map, accessible-name computation, tab-sequence derivation, action dispatch) is framework-agnostic tree manipulation. Keeping it in with no UI dependency lets the testing package, the Playwright adapter, and downstream tooling use the engine without pulling Preact into their bundle."
    code "@real-a11y-dev/core"
  heading "React-specific concerns live in @real-a11y-dev/react" (level 3)
    code "@real-a11y-dev/react"
  paragraph "React 18 concurrent-mode safety requires ; SSR (Next.js App Router) requires a mount-gated portal; React 19's jsx-runtime has internals that don't live in React 18. All of that React-specific complexity is isolated to one package with a clear React peer dep. Vanilla and Vue projects pay none of that cost — they use directly."
    code "useSyncExternalStore"
    code "inspector"
  heading "Testing is fully headless" (level 3)
  paragraph "is used by CI jobs that run in Node with no browser, by Vitest unit suites with jsdom, and by Playwright E2E jobs with a real Chromium. One API, three runtimes. Decoupling the UI makes that possible — the tree view isn't involved in any assertion path."
    code "@real-a11y-dev/testing"
  heading "The findings engine has one home" (level 3)
  paragraph "A finding — "this button has no accessible name" — is defined and detected in exactly one place: . The type, the rule set, , and the primitives all live there, depending on nothing but . Everything downstream only what the engine produces: re-exports it for test authors, and / format it for agents and the shell. Keeping detection separate from presentation means a new rule is written once and every surface reports it, and a production package like never has to pull in a test-helper package to reach the engine. is internal, so nobody installs it — it is inlined into , , , and , and the vocabulary it defines (, , , , the primitives) is published by 's re-export, with also by 's. That re-export is the contract, not a shortcut."
    code "@real-a11y-dev/audit"
    code "Finding"
    code "collectFindings"
    code "assert*"
    code "core"
    emphasis "renders"
    code "testing"
    code "mcp"
    code "cli"
    code "cli"
    code "audit"
    code "testing"
    code "browser"
    code "cli"
    code "mcp"
    code "Finding"
    code "A11yRule"
    code "ALL_RULES"
    code "collectFindings"
    code "assert*"
    code "testing"
    code "Finding"
    code "mcp"
  heading "The snapshot engine has one home" (level 3)
  paragraph "Like a finding, a — the diffable , its frozen fingerprints, and the diff over them — is built and compared in exactly one place: , Node-only and depending on nothing but , , and . The CLI and the MCP server both capture through it and diff through it, so a snapshot taken by one and compared by the other is byte-for-byte identical: fingerprint parity stops being a discipline ("both surfaces must build the artifact the same way") and becomes structural (there is only one place the code lives). That one place is now internal, which settles what the CLI is: there is no library to import, so the engine is reached by running it. and both take and , so the artifact and the diff stay machine-readable — you get them out of a command instead of an import, and the MCP checkpoint tools are the same engine behind a different surface. The CLI ships just its ."
    emphasis "snapshot"
    code "a11y-snapshot.json"
    code "v1:"
    code "@real-a11y-dev/snapshot"
    code "audit"
    code "serialize"
    code "core"
    code "real-a11y snapshot"
    code "real-a11y diff"
    code "--format json"
    code "-o"
    code "bin"
  heading "The real browser lives in one place" (level 3)
  paragraph "Driving a real Chromium — launching Playwright, injecting the page-bundle, marshalling calls across — is the one genuinely heavyweight dependency in the stack. It all lives in : the and the injected bundle it ships, inlining / and depending on plus an peer. The CLI, the MCP server, and the testing Playwright adapter all drive the browser through this single package, so there is exactly one place the bundle is built and one contract for injecting it — a tree captured by any of the three is identical. Everything above is browserless and Node- or jsdom-safe; a consumer that only needs the engine never pulls Playwright into its graph. That package is internal now, which changes who installs it — nobody. The CLI, the server, and the testing adapter each inline it, so is an optional peer of whichever of the three you installed. re-exports and the session types (, , , ) so an embedder can build a session to hand ; the rest of the driver — , , , the Chrome-resolution helpers — is reached by running a surface, not importing one."
    code "page.evaluate()"
    code "@real-a11y-dev/browser"
    code "BrowserSession"
    code "audit"
    code "serialize"
    code "core"
    emphasis "optional"
    code "playwright"
    code "browser"
    code "playwright"
    code "mcp"
    code "BrowserSession"
    code "A11ySession"
    code "BrowserSessionOptions"
    code "PageSnapshot"
    code "SnapshotOptions"
    code "buildServer"
    code "nativeTree"
    code "resolveTarget"
    code "pageBundleSource"
  heading "Internal packages are bundled, not shipped separately" (level 3)
  paragraph "and could each have been a normal dependency — both were, through and . Nobody was ever told to install them: the only lines were in their own READMEs, and no published declaration named their types. That second half is what separates them from the three that followed. What a consumer wants is the exact version the parent package was tested against, which is what already gave them; making that official removes an entire class of peer-range support questions and lets both refactor freely inside any release that updates their consumers. The rule the split now follows: a package is published because someone installs it on purpose, not because the build happens to split there — and that reads on the rest of the graph too, since an engine seam is a seam whether or not it has a registry entry."
    code "@real-a11y-dev/semantic-navigator-ui"
    code "@real-a11y-dev/validate"
    code "0.1.0-beta.11"
    code "0.1.0-beta.7"
    code "npm install"
    code "noExternal"
  paragraph "That is what happened next: , , and — published through — went internal for the same reason. They differ from the first two in the one way that mattered: their types named by published declarations, so the move had to keep the vocabulary reachable rather than merely stop shipping a tarball. re-exports , , , , the primitives, , , and the three snapshot serializers — is the only published home for any of it. had nothing to re-export — its surface is an engine you run, not a vocabulary you name — so it reaches users as the CLI's / commands and the MCP checkpoint tools, and anyone who was importing it has no drop-in."
    code "serialize"
    code "audit"
    code "snapshot"
    code "0.1.0-beta.12"
    emphasis "were"
    code "testing"
    code "Finding"
    code "A11yRule"
    code "ALL_RULES"
    code "collectFindings"
    code "assert*"
    code "extract"
    code "SerializeOptions"
    code "testing"
    code "snapshot"
    code "snapshot"
    code "diff"
  paragraph "was next, through , and it makes the rule read on the heaviest package in the tree rather than the lightest. It is the only one that touches Playwright and the only one that ships a page-bundle, but nobody arrived at it on purpose either: every consumer met it through the CLI, the MCP server, or the testing adapter, and the peer-dependency question it raised (, optional) was always really theirs to answer. Five names keep a published home — re-exports with , , and , because takes an and a contract you cannot name is a contract you cannot implement. The rest of the driver has no drop-in: it is something you run, through , the MCP tools, or in a Playwright suite."
    code "browser"
    code "0.1.0-beta.13"
    code "playwright"
    code "mcp"
    code "BrowserSession"
    code "A11ySession"
    code "BrowserSessionOptions"
    code "PageSnapshot"
    code "SnapshotOptions"
    code "buildServer"
    code "A11ySession"
    code "real-a11y"
    code "attach(page)"
  paragraph "came last, and it is the one that tests whether the rule was ever really about size. It is the engine — every other package in the graph is a projection of what it returns — and it was the first thing published, back when "the engine" and "the product" were the same sentence. They stopped being the same sentence a while ago: nobody installs an extraction engine, they install a matcher, a panel, a command, or a server, and the engine arrives inside it. Published through ."
    code "core"
    code "0.1.0-beta.13"
  paragraph "Nineteen of its sixty-nine names keep a published home, because the packages above genuinely name them: carries the query and diff vocabulary (, , , , , ) along with , and ; and carry the node, action and config types their own signatures use. The other fifty have no drop-in, and two of them are worth naming: and , the extractors themselves. Building your own published tooling directly on the engine was a documented path, and it is not one any more — the honest replacement is a surface you run (, the MCP tools) or a package that already wraps the engine for your context. That is a real capability leaving, stated rather than glossed: it is the cost of the rule, and beta is when a cost like that is cheapest to take."
    code "testing"
    code "findByRole"
    code "findAllByRole"
    code "diffTrees"
    code "getOutline"
    code "getTabSequence"
    code "linearize"
    code "SemanticNode"
    code "ExtractionResult"
    code "TreeDiff"
    code "react"
    code "inspector"
    code "extractA11yTree"
    code "extractDomTree"
    code "real-a11y"
  paragraph "Bundling a private package is two halves, not one. inlines the JS; inlines the declarations. Skip the second and the emitted still says — a specifier npm cannot resolve, which is for a consumer, or, under , types silently degrading to . fails on exactly that, so it is enforced rather than remembered."
    code "noExternal"
    code "dts.resolve"
    code ".d.ts"
    code "from "@real-a11y-dev/validate""
    code "TS2307"
    code "skipLibCheck: true"
    code "any"
    code "surface:check"
  separator
  heading "Build pipeline per package" (level 2)
  paragraph "Every package uses the same tsup config shape:"
  list
    listitem "(, )"
      strong "ESM + CJS dual output"
      code "dist/index.js"
      code "dist/index.cjs"
    listitem "(, )"
      strong "Type declarations"
      code "dist/index.d.ts"
      code "dist/index.d.cts"
    listitem "for debuggable stack traces in downstream test runners"
      strong "Source maps"
    listitem "— only the built output ships to npm; source, configs, and tests stay in the repo"
      strong
        code ""files": ["dist"]"
  paragraph "Per-entrypoint specifics:"
  list
    listitem "— two entries (, ). The Playwright entry carries the IIFE page-bundle as inlined source text from the internal package and injects it by evaluating that source — not , which a strict CSP blocks."
      strong
        code "@real-a11y-dev/testing"
      code "index"
      code "playwright"
      code "browser"
      code "page.addScriptTag()"
      code "script-src"
    listitem "— three entries (, , ). The manager entry forces classic JSX transform so Storybook's React-externalization works; see ."
      strong
        code "@real-a11y-dev/storybook-addon"
      code "index"
      code "preview"
      code "manager"
      link "packages/storybook-addon/tsup.config.ts"
        code "packages/storybook-addon/tsup.config.ts"
    listitem "— and the addon's entry set for (plus ); does it for , , , and ; itself for and ; and for , , , , and . Any entry whose declarations could a private package pairs with — , , , the addon's — because otherwise the shipped keeps an import npm cannot resolve. is the sharpest case: re-exports five names straight , so the resolve entry is what turns them into inlined declaration text instead of a specifier npm has no copy of. pairs them pre-emptively: its entries are the and the daemon, so the emit is currently just a shebang — but nothing watches it (no map for to read, and skips it), so the resolve list is there before it is needed rather than after."
      strong "Internal packages are inlined"
      code "inspector"
      code "manager"
      code "noExternal"
      code "@real-a11y-dev/semantic-navigator-ui"
      code "core"
      code "testing"
      code "browser"
      code "validate"
      code "audit"
      code "serialize"
      code "browser"
      code "audit"
      code "serialize"
      code "cli"
      code "mcp"
      code "browser"
      code "session-registry"
      code "audit"
      code "serialize"
      code "snapshot"
      emphasis "name"
      code "noExternal"
      code "dts: { resolve: [...] }"
      code "testing"
      code "browser"
      code "mcp"
      code "manager"
      code ".d.ts"
      code "mcp"
      code "server.d.ts"
      emphasis "from"
      code "browser"
      code "cli"
      code "bin"
      code "exports"
      code "surface:check"
      code "ATTW_SKIP_PACKAGES"
  separator
  heading "The SemanticNode data model" (level 2)
  paragraph "All packages share a single node shape from . See for the full schema. The contract:"
    code "@real-a11y-dev/core"
    link "Core Concepts"
  button "Copy Code"
  code
  paragraph "Everything else — the UI, the assertions, the snapshots, the Storybook panel — is a projection of a . When a consumer finds a surprising output, the conversation terminates at "what does return?" That's the root of every question."
    code "Map<string, SemanticNode>"
    code "extractA11yTree(root)"
  separator
  heading "Where to read next" (level 2)
  list
    listitem "— the semantic tree model, roles, tab order"
      link "Core Concepts"
    listitem "— the ANDC algorithm as implemented in"
      link "Accessible Names"
      code "core"
contentinfo
  link "Edit this page on GitHub"
  paragraph "Last updated:"
    time "[redacted]"
  navigation "Pager"
    link "Previous pageAccessible Names"
    link "Next pageTroubleshooting"