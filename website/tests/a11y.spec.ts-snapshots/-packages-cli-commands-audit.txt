link "Skip to content"
banner
  link "Real A11y"
  button "Search"
  navigation "Main Navigation"
    link "Guide"
    link "Recipes"
    link "Packages"
    button "v0.1 · Beta"
  switch "Switch to dark theme"
  link "github"
complementary
  navigation "Sidebar Navigation"
    button "Introduction"
      heading "Introduction" (level 2)
    link "Getting Started"
      paragraph "Getting Started"
    link "Core Concepts"
      paragraph "Core Concepts"
    link "Accessibility Snapshots"
      paragraph "Accessibility Snapshots"
    link "Snapshots vs. Other Tools"
      paragraph "Snapshots vs. Other Tools"
    link "Why Real A11y?"
      paragraph "Why Real A11y?"
    button "Reading the Tree"
      heading "Reading the Tree" (level 2)
    link "Understanding the Views"
      paragraph "Understanding the Views"
    link "DOM View"
      paragraph "DOM View"
    link "A11y View"
      paragraph "A11y View"
    link "Headings View"
      paragraph "Headings View"
    link "TAB View"
      paragraph "TAB View"
    link "Panel features"
      paragraph "Panel features"
    button "Chrome Extension"
      heading "Chrome Extension" (level 2)
    link "Overview & Install"
      paragraph "Overview & Install"
    button "Packages"
      heading "Packages" (level 2)
    link "@real-a11y-dev/core"
      paragraph "@real-a11y-dev/core"
    link "@real-a11y-dev/inspector"
      paragraph "@real-a11y-dev/inspector"
    link "@real-a11y-dev/testing"
      heading "@real-a11y-dev/testing" (level 3)
    button "toggle section"
    link "@real-a11y-dev/react"
      paragraph "@real-a11y-dev/react"
    link "@real-a11y-dev/storybook-addon"
      paragraph "@real-a11y-dev/storybook-addon"
    link "@real-a11y-dev/mcp"
      heading "@real-a11y-dev/mcp" (level 3)
    button "toggle section"
    link "@real-a11y-dev/cli"
      heading "@real-a11y-dev/cli" (level 3)
    button "toggle section"
    link "Commands & flags"
      paragraph "Commands & flags"
    link "Configuration"
      paragraph "Configuration"
    button "Recipes"
      heading "Recipes" (level 2)
    link "Next.js (App Router + React 19)"
      paragraph "Next.js (App Router + React 19)"
    link "Storybook 8 + React 19"
      paragraph "Storybook 8 + React 19"
    link "Peer Dependencies"
      paragraph "Peer Dependencies"
    link "CI Diff Bot"
      paragraph "CI Diff Bot"
    button "Reference"
      heading "Reference" (level 2)
    link "Authenticated Pages"
      paragraph "Authenticated Pages"
    link "Accessible Names"
      paragraph "Accessible Names"
    link "Architecture"
      paragraph "Architecture"
    link "Troubleshooting"
      paragraph "Troubleshooting"
navigation "On this page"
  heading "On this page" (level 2)
  list
    listitem
      link "All commands at a glance"
    listitem
      link "Commands"
    listitem
      link "Flags"
    listitem
      link "Browser & page"
    listitem
      link "Output"
    listitem
      link "Config"
    listitem
      link "Gate"
    listitem
      link "Command-specific"
main
  heading "Commands & flags" (level 1)
  paragraph "Every invocation is . Fourteen commands ship — , , , , , , , , , , , , , and . Run for a command's own flags."
    code "real-a11y <command> [url...] [flags]"
    link "install"
      code "install"
    link "audit"
      code "audit"
    link "inspect"
      code "inspect"
    link "tree"
      code "tree"
    link "outline"
      code "outline"
    link "tabs"
      code "tabs"
    link "list"
      code "list"
    link "interact"
      code "interact"
    link "click"
      code "click"
    link "type"
      code "type"
    link "focus"
      code "focus"
    link "snapshot"
      code "snapshot"
    link "diff"
      code "diff"
    link "login"
      code "login"
    code "real-a11y <command> --help"
  paragraph "Findings and reports go to ; progress, warnings, and errors go to — so / a pipe never mixes the two."
    strong "stdout"
    strong "stderr"
    code "-o"
  paragraph
    strong "Exit codes are frozen:"
  table
    rowgroup
      row
        columnheader "Code"
        columnheader "Meaning"
    rowgroup
      row
        cell "0"
          code "0"
        cell "Clean — no findings at or above --fail-on ."
          link "--fail-on"
            code "--fail-on"
      row
        cell "1"
          code "1"
        cell "Findings at or above the threshold (the CI gate)."
      row
        cell "2"
          code "2"
        cell "Usage or navigation error."
  paragraph "View commands (, , , ) aren't gates — they exit unless something actually failed. Neither are the act commands (, , , ): they exit when every step lands, and when one can't be reached."
    link "tree"
      code "tree"
    link "outline"
      code "outline"
    link "tabs"
      code "tabs"
    link "list"
      code "list"
    code "0"
    link "interact"
      code "interact"
    link "click"
      code "click"
    link "type"
      code "type"
    link "focus"
      code "focus"
    code "0"
    code "2"
  paragraph
    strong "Environment variables:"
  list
    listitem "— a JSON array, the page set for and when no URL is passed."
      strong
        code "A11Y_PAGES"
      code "[{ "name": …, "url": … }]"
      link "audit"
        code "audit"
      link "snapshot"
        code "snapshot"
    listitem "— default output path for when is omitted."
      strong
        code "A11Y_SNAPSHOT_OUT"
      link "snapshot"
        code "snapshot"
      link "-o"
        code "-o"
  paragraph "Human () output is and may be reworded between releases. The machine formats — , , , , and the fingerprints — are a : within , changes are additive-only. Never rely on the wording of a line; key on the JSON."
    code "pretty"
    strong "English-only"
    code "json"
    code "sarif"
    code "junit"
    code "jsonl"
    code "v1:"
    strong "frozen contract"
    code "0.x"
    code "pretty"
  heading "All commands at a glance" (level 2)
  paragraph "Every browser-driving command reads over CDP, except — the one view that tree cannot produce (see ). Click a command for its flags."
    strong "Chromium's own accessibility tree"
    link "tabs"
      code "tabs"
    link "--root"
      code "--root"
  paragraph
    strong "Setup"
  table
    rowgroup
      row
        columnheader "Command"
        columnheader "Purpose"
    rowgroup
      row
        cell "install"
          link "install"
            code "install"
        cell "Download Chrome for Testing into Real A11y's own cache — run once."
      row
        cell "login <url> --save <file>"
          link "login <url> --save <file>"
            code "login <url> --save <file>"
        cell "Log in by hand and save the session for --storage-state."
          code "--storage-state"
  paragraph "— the gates: these exit on findings at or above ."
    strong "Audit"
    code "1"
    link "--fail-on"
      code "--fail-on"
  table
    rowgroup
      row
        columnheader "Command"
        columnheader "Purpose"
    rowgroup
      row
        cell "audit <url...>"
          link "audit <url...>"
            code "audit <url...>"
        cell "Every violation, grouped by rule with locator + severity — the flagship."
      row
        cell "inspect <url>"
          link "inspect <url>"
            code "inspect <url>"
        cell "Findings plus tree + outline from one read."
          strong "plus"
  paragraph "— never gates; they exit unless something actually failed."
    strong "Views"
    code "0"
  table
    rowgroup
      row
        columnheader "Command"
        columnheader "Purpose"
    rowgroup
      row
        cell "tree <url>"
          link "tree <url>"
            code "tree <url>"
        cell "The semantic tree — role + accessible name, as a screen reader traverses it."
      row
        cell "outline <url>"
          link "outline <url>"
            code "outline <url>"
        cell "Heading outline (h1–h6) in document order."
      row
        cell "tabs <url>"
          link "tabs <url>"
            code "tabs <url>"
        cell "Focusable elements in keyboard Tab order — the one in-page read."
      row
        cell "list <category> <url>"
          link "list <category> <url>"
            code "list <category> <url>"
        cell "One category — heading, link, button, form, landmark, image."
  paragraph "— Chromium only. Exit when every step lands, when one can't be reached."
    strong "Act"
    code "0"
    code "2"
  table
    rowgroup
      row
        columnheader "Command"
        columnheader "Purpose"
    rowgroup
      row
        cell "interact <url> --step"
          link "interact <url> --step"
            code "interact <url> --step"
        cell "Run ordered steps, then print the tree diff they produced."
      row
        cell "click <url> --role"
          link "click <url> --role"
            code "click <url> --role"
        cell "Real click at the element matched by role + accessible name."
      row
        cell "type <url> --role --text"
          link "type <url> --role --text"
            code "type <url> --role --text"
        cell "Replace a text field's value; the value is never echoed back."
      row
        cell "focus <url> --role"
          link "focus <url> --role"
            code "focus <url> --role"
        cell "Move real keyboard focus; the [focused] marker moves in the diff."
          code "[focused]"
  paragraph
    strong "Artifacts"
  table
    rowgroup
      row
        columnheader "Command"
        columnheader "Purpose"
    rowgroup
      row
        cell "snapshot [url...]"
          link "snapshot [url...]"
            code "snapshot [url...]"
        cell "Audit a page set → one diffable JSON artifact (or --md)."
          code "--md"
      row
        cell "diff <base.json> <pr.json>"
          link "diff <base.json> <pr.json>"
            code "diff <base.json> <pr.json>"
        cell "Findings-aware diff of two artifacts — new / changed / fixed. Pure: no browser."
  heading "Commands" (level 2)
  paragraph "Each command below lists the flag it accepts. Shared flags are documented once, in — the per-command list only links to them, plus any command-specific flags."
    strong "groups"
    link "Flags"
  heading "install" (level 3)
    code "install"
  paragraph "Download — Google's versioned, non-auto-updating Chrome build — into Real A11y's own cache and use it for every launched session from then on. A setup command: it takes no URL and doesn't drive a browser itself. Idempotent — a bare re-run does zero network work and exits instantly when the cached build is still present. Prints the resolved executable path on ; progress goes to stderr."
    link "Chrome for Testing"
    code "0"
    strong "stdout"
  button "Copy Code"
  code
  paragraph "Playwright (the npm package) is still the driver — this replaces only the browser download. Every browser-driving command then picks it up automatically; see for the full resolution order. Sessions launched with attach to your own Chrome and never use this binary."
    code "npx playwright install chromium"
    link "--chrome-path"
      code "--chrome-path"
    link "--cdp"
      code "--cdp"
  paragraph "· · · · · · ."
    strong "Flags:"
    link "--channel"
      code "--channel"
    link "--version"
      code "--version"
    link "--force"
      code "--force"
    link "Config"
    link "-q, --quiet"
      code "-q, --quiet"
    link "--verbose"
      code "--verbose"
    link "-h, --help"
      code "-h, --help"
  heading "audit <url...>" (level 3)
    code "audit <url...>"
  paragraph "The flagship gate. Audits one or more pages against the semantic-tree rules and prints violations grouped by rule — each with a CSS and . Exits on errors with no extra flags. A failed page becomes an entry (exit ) while the others still report. Under GitHub Actions it also emits grouped annotations and a job summary."
    strong "locator"
    strong "severity"
    code "1"
    code "error"
    code "2"
    code "::error"
  paragraph "With no URL, it audits every entry in or the config's ."
    code "A11Y_PAGES"
    code "urls"
  button "Copy Code"
  code
  paragraph "· · · · (default ) · · no ."
    strong "Flags:"
    link "Browser & page"
    link "Output"
    link "Config"
    link "--rules"
      code "--rules"
    link "--fail-on"
      code "--fail-on"
    code "error"
    link "--no-annotate"
      code "--no-annotate"
    code "--root"
  heading "inspect <url>" (level 3)
    code "inspect <url>"
  paragraph "Findings the semantic tree and heading outline — all from read of Chromium's own accessibility tree, so the views can never disagree, and the findings always agree with . Views print first; the gate outcome is the last thing on screen. Single URL."
    strong "plus"
    strong "one"
    link "audit"
      code "audit"
  paragraph "That tree carries no tab order, so this command prints none — and prints no empty section either, which would read as . Run for the sequence."
    emphasis "nothing here is focusable"
    link "tabs"
      code "tabs"
  button "Copy Code"
  code
  paragraph "· · · · (default ) · · ."
    strong "Flags:"
    link "Browser & page"
    link "Output"
    link "Config"
    link "--rules"
      code "--rules"
    link "--fail-on"
      code "--fail-on"
    code "error"
    link "--no-annotate"
      code "--no-annotate"
    link "--include-generic"
      code "--include-generic"
  heading "tree <url>" (level 3)
    code "tree <url>"
  paragraph "Print the semantic tree — what a screen reader perceives, role by role. Single URL; always exits ."
    code "0"
  button "Copy Code"
  code
  paragraph "· () · · · no ."
    strong "Flags:"
    link "Browser & page"
    link "Output"
    code "pretty | json"
    link "Config"
    link "--include-generic"
      code "--include-generic"
    code "--root"
  heading "outline <url>" (level 3)
    code "outline <url>"
  paragraph "Print the heading outline (h1–h6) in document order. Single URL; always exits ."
    code "0"
  button "Copy Code"
  code
  paragraph "· () · · no ."
    strong "Flags:"
    link "Browser & page"
    link "Output"
    code "pretty | json"
    link "Config"
    code "--root"
  heading "tabs <url>" (level 3)
    code "tabs <url>"
  paragraph "Print every focusable element in keyboard order. Single URL; always exits ."
    strong "Tab"
    code "0"
  paragraph "The one command still built from the , and the only one that takes . Chromium's accessibility tree knows whether a node is focusable, but not the — never reaches a native node — so tab order is DOM work by nature, not a fallback."
    strong "in-page DOM walk"
    link "--root"
      code "--root"
    emphasis "sequence"
    code "tabindex"
  button "Copy Code"
  code
  paragraph "· () · · ."
    strong "Flags:"
    link "Browser & page"
    link "Output"
    code "pretty | json"
    link "Config"
    link "--root"
      code "--root"
  heading "list <category> <url>" (level 3)
    code "list <category> <url>"
  paragraph "List every element in one category — as role, accessible name, and locator. The category is the first positional; the URL follows. Single URL; always exits ."
    code "0"
  paragraph "Categories: , , , , , ."
    code "heading"
    code "link"
    code "button"
    code "form"
    code "landmark"
    code "image"
  button "Copy Code"
  code
  paragraph "An empty category reports how much it scanned and which roles it looked for, so "none" is never confusable with a page that failed to load:"
  button "Copy Code"
  code
  paragraph "Worth knowing when a category surprises you: looks for exactly , so a page whose graphics are s lists none, and includes the role while does not — that one lists the fields."
    code "image"
    code "img"
    code "figure"
    code "landmark"
    code "form"
    code "form"
  paragraph "· () · ."
    strong "Flags:"
    link "Browser & page"
    link "Output"
    code "pretty | json"
    link "Config"
  heading "interact <url> --step '<step>'" (level 3)
    code "interact <url> --step '<step>'"
  paragraph "Drive a page through one or more steps, then print the those steps produced — the answer to "what did that actually change for a screen reader?". Steps run in order and stop at the first failure. Single URL; exits when every step lands, on a usage error or an unreachable target."
    strong "accessibility-tree diff"
    code "0"
    code "2"
    strong "Chromium only."
  paragraph "A step is written the way the tree prints a node:"
  button "Copy Code"
  code
  list
    listitem "— , , ."
      strong "verbs"
      code "click"
      code "type"
      code "focus"
    listitem "— the accessible name. Omit it to match any name; pass to target an control (the one an audit just flagged)."
      strong
        code ""<name>""
      code """"
      strong "unlabeled"
    listitem "— 1-based, document order. It's the spelling the ambiguity error prints, so the fix is copy-paste."
      strong
        code "nth=<n>"
    listitem "— only. Everything after the first is the value, so query strings and base64 need no escaping."
      strong
        code "= <text>"
      code "type"
      code "="
  button "Copy Code"
  code
  paragraph "Targets resolve against accessibility tree by role + accessible name — never a CSS selector. If a control can't be reached that way, assistive technology can't reach it either, and that is an accessibility finding rather than a targeting inconvenience. Ambiguous matches list their candidates; a target is refused, because the click would be swallowed and the resulting empty diff would read as "that button does nothing"."
    strong "Chromium's own"
    code "nth="
    strong "disabled"
  paragraph "The actions are real"
  paragraph "They submit forms, toggle state, and can . A step that loads a new document leaves the tree captured before it describing a page that no longer exists, so no diff is possible — the run still succeeds and reports where it landed."
    strong "navigate"
  paragraph "Targeting, acting, and the diff all read the tree — Chromium's own, over CDP. A node you aim at by one name therefore can't come back in the report under another. That tree is whole-document, which is why these commands take no ."
    strong "same"
    link "--root"
      code "--root"
  paragraph "A typed value is — not in progress output, not in , where the step renders as . Don't use to log in: a password on the command line is visible to other processes and lands in your shell history. Use ."
    strong "never echoed"
    link "--format json"
      code "--format json"
    code "= ‹hidden›"
    code "type"
    link "login"
      code "login"
  paragraph "Under the page object carries (the steps that ran, rendered and redacted), , and — the last so a script can tell a new document was loaded (so there is no diff) without matching prose. is the address the page on, which differs from the one you passed when a step navigated."
    link "--format json"
      code "--format json"
    code "steps"
    code "diff"
    code "navigated"
    code "url"
    strong "landed"
  paragraph "(repeatable, required) · · (no ) · () · ."
    strong "Flags:"
    code "--step '<step>'"
    link "--step-settle"
      code "--step-settle"
    link "Browser & page"
    code "--root"
    link "Output"
    code "pretty | json"
    link "Config"
  heading "click <url> --role <role>" (level 3)
    code "click <url> --role <role>"
  paragraph "Dispatch a real click at the element matched by role + accessible name, then print the tree diff. Shorthand for a one-step , and bound by the same contract."
    link "interact"
      code "interact"
  button "Copy Code"
  code
  paragraph "(required) · · · · (no ) · () · ."
    strong "Flags:"
    code "--role"
    code "--name"
    code "--nth"
    link "--step-settle"
      code "--step-settle"
    link "Browser & page"
    code "--root"
    link "Output"
    code "pretty | json"
    link "Config"
  heading "type <url> --role <role> --text <value>" (level 3)
    code "type <url> --role <role> --text <value>"
  paragraph "Replace a text field's value (role is usually , , or ), then print the tree diff — a combobox popping its options, an inline validation error appearing. The value is written through the element's own prototype setter plus / events, so framework-controlled inputs (React et al.) register it."
    code "textbox"
    code "searchbox"
    code "combobox"
    code "input"
    code "change"
  button "Copy Code"
  code
  paragraph "The value is never echoed back, in any format. Don't use it to log in — see ."
    link "login"
      code "login"
  paragraph "(required) · (required) · · · · (no ) · () · ."
    strong "Flags:"
    code "--role"
    code "--text"
    code "--name"
    code "--nth"
    link "--step-settle"
      code "--step-settle"
    link "Browser & page"
    code "--root"
    link "Output"
    code "pretty | json"
    link "Config"
  heading "focus <url> --role <role>" (level 3)
    code "focus <url> --role <role>"
  paragraph "Move real keyboard focus to the matched element, then print the tree diff — the focus move shows as the marker relocating. Pairs with for focus-order work."
    code "[focused]"
    link "tabs"
      code "tabs"
  button "Copy Code"
  code
  paragraph "(required) · · · · (no ) · () · ."
    strong "Flags:"
    code "--role"
    code "--name"
    code "--nth"
    link "--step-settle"
      code "--step-settle"
    link "Browser & page"
    code "--root"
    link "Output"
    code "pretty | json"
    link "Config"
  heading "snapshot [url...]" (level 3)
    code "snapshot [url...]"
  paragraph "Audit a page set and write JSON artifact — fingerprinted findings plus the tree and outline views per page, with recording which views the run measured. That artifact is the input to ."
    strong "one"
    code "meta.views"
    link "diff"
      code "diff"
  paragraph "There is no tabs view: this reads Chromium's whole-document accessibility tree, which carries no tab order. The artifact omits the view rather than storing an empty one, so a diff reads it as instead of . is the keyboard sequence."
    emphasis "not measured"
    emphasis "every tab stop was removed"
    code "real-a11y tabs"
  paragraph "Pages, in precedence order: positional URLs, else , else the config's ( or auto-discovered). Output goes to , else , else stdout. Unlike the other gates, defaults to here — snapshot just writes the artifact unless you ask it to gate."
    code "A11Y_PAGES"
    code "urls"
    link "--config"
      code "--config"
    link "-o"
      code "-o"
    code "A11Y_SNAPSHOT_OUT"
    link "--fail-on"
      code "--fail-on"
    code "never"
  button "Copy Code"
  code
  paragraph "· · · (default ) · · () · · (, md-report-only) · · · · · · ."
    strong "Flags:"
    link "Browser & page"
    link "Config"
    link "--rules"
      code "--rules"
    link "--fail-on"
      code "--fail-on"
    code "never"
    link "--include-generic"
      code "--include-generic"
    link "-f, --format"
      code "-f, --format"
    code "json | md | sarif | junit | jsonl"
    link "--md"
      code "--md"
    link "--only"
      code "--only"
    code "findings | views"
    link "--baseline"
      code "--baseline"
    link "--update-baseline"
      code "--update-baseline"
    link "-o, --output"
      code "-o, --output"
    link "-q, --quiet"
      code "-q, --quiet"
    link "--verbose"
      code "--verbose"
    link "-h, --help"
      code "-h, --help"
  heading "diff <base.json> <pr.json>" (level 3)
    code "diff <base.json> <pr.json>"
  paragraph "Classify the findings in two snapshot artifacts as — robust to DOM churn (re-indentation, renumbered locators) that defeats a line diff. Pure: no browser. Takes exactly two positional files. Exits on NEW findings at or above ; fixes and drift never fail the build."
    strong "new / changed / fixed"
    code "1"
    strong "only"
    link "--fail-on"
      code "--fail-on"
  paragraph "Default output is neutral — findings plus a real unified diff of the structure. Add for a plain-language summary, or report a single axis with (an output filter — the exit gate is unchanged)."
    link "--explain"
      code "--explain"
    link "--only findings | views"
      code "--only findings | views"
  paragraph "Pages are matched by their , never by URL — base and PR legitimately run on different hosts and ports. A page whose name is on only one side is reported as added or removed and is never compared. Since a bare entry (or a positional URL) takes the URL as its name, snapshotting the two sides from different origins leaves nothing to join on: give the pages explicit names (config entries, or as ) so both runs agree. If no name matches at all, warns on stderr that it compared nothing — the report and exit code are unaffected."
    code "name"
    code "urls"
    code "snapshot"
    code "urls"
    code "A11Y_PAGES"
    code "[{ name, url }]"
    code "diff"
  button "Copy Code"
  code
  paragraph "· (default ) · · () · · · · · () · · · · ."
    strong "Flags:"
    link "Config"
    link "--fail-on"
      code "--fail-on"
    code "error"
    link "--explain"
      code "--explain"
    link "--only"
      code "--only"
    code "findings | views"
    link "--max-lines"
      code "--max-lines"
    link "--max-pages"
      code "--max-pages"
    link "--baseline"
      code "--baseline"
    link "--ignore-view-line"
      code "--ignore-view-line"
    link "-f, --format"
      code "-f, --format"
    code "pretty | json | md"
    link "-o, --output"
      code "-o, --output"
    link "-q, --quiet"
      code "-q, --quiet"
    link "--verbose"
      code "--verbose"
    link "-h, --help"
      code "-h, --help"
  heading "login <url> --save <file>" (level 3)
    code "login <url> --save <file>"
  paragraph "Open a visible browser, log in by hand — MFA, SSO, and passkeys all work, because a human is driving — then press to save the session to a file you can later pass to . Interactive by construction: it needs a TTY and fails fast in CI. Forces headful; no device/viewport emulation, format, or output flags. The saved file holds live session tokens — keep it out of version control (the command warns if it isn't)."
    strong "Enter"
    link "--storage-state"
      code "--storage-state"
  button "Copy Code"
  code
  paragraph "(required) · · · · · · ."
    strong "Flags:"
    link "--save"
      code "--save"
    link "--wait-until"
      code "--wait-until"
    link "--settle"
      code "--settle"
    link "--timeout"
      code "--timeout"
    link "Config"
    link "--verbose"
      code "--verbose"
    link "-h, --help"
      code "-h, --help"
  heading "Flags" (level 2)
  paragraph "Grouped and documented once. The line above each entry names which commands accept it. An explicit flag always wins over an default; see ."
    strong "Commands"
    code "a11y.config.json"
    link "Configure once"
  heading "All flags at a glance" (level 3)
  paragraph "Throughout this table, is the eleven that drive a page: , , , , , , , , , , ."
    strong "browser commands"
    link "audit"
      code "audit"
    link "inspect"
      code "inspect"
    link "tree"
      code "tree"
    link "outline"
      code "outline"
    link "tabs"
      code "tabs"
    link "list"
      code "list"
    link "interact"
      code "interact"
    link "click"
      code "click"
    link "type"
      code "type"
    link "focus"
      code "focus"
    link "snapshot"
      code "snapshot"
  paragraph
    strong
      link "Browser & page"
  table
    rowgroup
      row
        columnheader "Flag"
        columnheader "Type / values"
        columnheader "Default"
        columnheader "Commands"
    rowgroup
      row
        cell "--root"
          link "--root"
            code "--root"
        cell "CSS selector"
        cell "body"
          code "body"
        cell "tabs only †"
          link "tabs"
            code "tabs"
          superscript "†"
      row
        cell "--device"
          link "--device"
            code "--device"
        cell "Playwright device name"
        cell "none"
        cell "browser commands"
      row
        cell "--viewport"
          link "--viewport"
            code "--viewport"
        cell "WIDTHxHEIGHT"
          code "WIDTHxHEIGHT"
        cell "none"
        cell "browser commands"
      row
        cell "--wait-until"
          link "--wait-until"
            code "--wait-until"
        cell "load | domcontentloaded | networkidle | commit"
          code "load | domcontentloaded | networkidle | commit"
        cell "load"
          code "load"
        cell "browser commands · login"
          code "login"
      row
        cell "--settle"
          link "--settle"
            code "--settle"
        cell "ms"
        cell "0"
          code "0"
        cell "browser commands · login"
          code "login"
      row
        cell "--step-settle"
          link "--step-settle"
            code "--step-settle"
        cell "ms"
        cell "200"
          code "200"
        cell "the act commands"
      row
        cell "--timeout"
          link "--timeout"
            code "--timeout"
        cell "ms"
        cell "30000"
          code "30000"
        cell "browser commands · login"
          code "login"
      row
        cell "--headful"
          link "--headful"
            code "--headful"
        cell "boolean"
        cell "false"
          code "false"
        cell "browser commands"
      row
        cell "--cdp"
          link "--cdp"
            code "--cdp"
        cell "CDP endpoint URL"
        cell "none"
        cell "browser commands"
      row
        cell "--chrome-path"
          link "--chrome-path"
            code "--chrome-path"
        cell "path to a browser binary"
        cell "none"
        cell "browser commands"
      row
        cell "--allow-file"
          link "--allow-file"
            code "--allow-file"
        cell "boolean"
        cell "false"
          code "false"
        cell "browser commands"
      row
        cell "--storage-state"
          link "--storage-state"
            code "--storage-state"
        cell "path to a saved session"
        cell "none"
        cell "browser commands"
      row
        cell "--audit-origin"
          link "--audit-origin"
            code "--audit-origin"
        cell "origin (repeatable)"
        cell "the target's own"
        cell "browser commands"
      row
        cell "--include-generic"
          link "--include-generic"
            code "--include-generic"
        cell "boolean"
        cell "false"
          code "false"
        cell "inspect, tree, outline, tabs, list, snapshot"
          code "inspect"
          code "tree"
          code "outline"
          code "tabs"
          code "list"
          code "snapshot"
  paragraph "Every other browser command reads Chromium's whole-document accessibility tree, so there is nothing for a selector to scope — they reject with that explanation rather than accepting and ignoring it. There is no producer flag either: each command has exactly one correct producer, so there is nothing to choose."
    superscript "†"
    code "--root"
  paragraph
    strong
      link "Output"
  table
    rowgroup
      row
        columnheader "Flag"
        columnheader "Type / values"
        columnheader "Default"
        columnheader "Commands"
    rowgroup
      row
        cell "-f, --format"
          link "-f, --format"
            code "-f, --format"
        cell "enum, per command"
        cell "first value"
        cell "browser commands · diff"
          code "diff"
      row
        cell "-o, --output"
          link "-o, --output"
            code "-o, --output"
        cell "path"
        cell "stdout"
        cell "browser commands · diff"
          code "diff"
      row
        cell "-q, --quiet"
          link "-q, --quiet"
            code "-q, --quiet"
        cell "boolean"
        cell "false"
          code "false"
        cell "browser commands · diff · install"
          code "diff"
          code "install"
      row
        cell "--verbose"
          link "--verbose"
            code "--verbose"
        cell "boolean"
        cell "false"
          code "false"
        cell "all"
      row
        cell "-h, --help"
          link "-h, --help"
            code "-h, --help"
        cell "boolean"
        cell "—"
        cell "all"
  paragraph
    strong
      link "Config"
  table
    rowgroup
      row
        columnheader "Flag"
        columnheader "Type / values"
        columnheader "Default"
        columnheader "Commands"
    rowgroup
      row
        cell "--config"
          link "--config"
            code "--config"
        cell "path to a11y.config.json"
          code "a11y.config.json"
        cell "auto-discovered"
        cell "all"
      row
        cell "--no-config"
          link "--no-config"
            code "--no-config"
        cell "boolean"
        cell "false"
          code "false"
        cell "all"
  paragraph
    strong
      link "Gate"
  table
    rowgroup
      row
        columnheader "Flag"
        columnheader "Type / values"
        columnheader "Default"
        columnheader "Commands"
    rowgroup
      row
        cell "--rules"
          link "--rules"
            code "--rules"
        cell "comma-separated rule ids"
        cell "all rules"
        cell "audit, inspect, snapshot"
          code "audit"
          code "inspect"
          code "snapshot"
      row
        cell "--fail-on"
          link "--fail-on"
            code "--fail-on"
        cell "error | warning | never"
          code "error | warning | never"
        cell "error (never for snapshot)"
          code "error"
          code "never"
          code "snapshot"
        cell "audit, inspect, snapshot, diff"
          code "audit"
          code "inspect"
          code "snapshot"
          code "diff"
      row
        cell "--no-annotate"
          link "--no-annotate"
            code "--no-annotate"
        cell "boolean"
        cell "false"
          code "false"
        cell "audit, inspect"
          code "audit"
          code "inspect"
  paragraph
    strong "Command-specific"
  table
    rowgroup
      row
        columnheader "Flag"
        columnheader "Type / values"
        columnheader "Default"
        columnheader "Commands"
    rowgroup
      row
        cell "--step"
          link "--step"
            code "--step"
        cell "step string (repeatable, ordered)"
        cell "—"
        cell "interact (required)"
          code "interact"
          strong "required"
      row
        cell "--role"
          link "--role"
            code "--role"
        cell "ARIA role"
        cell "—"
        cell "click, type, focus (required)"
          code "click"
          code "type"
          code "focus"
          strong "required"
      row
        cell "--name"
          link "--name"
            code "--name"
        cell "accessible name; "" = unlabeled"
          code """"
        cell "any name"
        cell "click, type, focus"
          code "click"
          code "type"
          code "focus"
      row
        cell "--nth"
          link "--nth"
            code "--nth"
        cell "positive integer, 1-based"
        cell "—"
        cell "click, type, focus"
          code "click"
          code "type"
          code "focus"
      row
        cell "--text"
          link "--text"
            code "--text"
        cell "the value to enter"
        cell "—"
        cell "type (required; click / focus reject it)"
          code "type"
          strong "required"
          code "click"
          code "focus"
      row
        cell "--channel"
          link "--channel"
            code "--channel"
        cell "stable | beta | dev | canary"
          code "stable | beta | dev | canary"
        cell "stable"
          code "stable"
        cell "install"
          code "install"
      row
        cell "--version"
          link "--version"
            code "--version"
        cell "exact Chrome build id"
        cell "—"
        cell "install"
          code "install"
      row
        cell "--force"
          link "--force"
            code "--force"
        cell "boolean"
        cell "false"
          code "false"
        cell "install"
          code "install"
      row
        cell "--save"
          link "--save"
            code "--save"
        cell "path"
        cell "—"
        cell "login (required)"
          code "login"
          strong "required"
      row
        cell "--md"
          link "--md"
            code "--md"
        cell "boolean"
        cell "false"
          code "false"
        cell "snapshot"
          code "snapshot"
      row
        cell "--only"
          link "--only"
            code "--only"
        cell "findings | views"
          code "findings | views"
        cell "both"
        cell "snapshot, diff"
          code "snapshot"
          code "diff"
      row
        cell "--baseline"
          link "--baseline"
            code "--baseline"
        cell "path"
        cell "none"
        cell "snapshot, diff"
          code "snapshot"
          code "diff"
      row
        cell "--update-baseline"
          link "--update-baseline"
            code "--update-baseline"
        cell "boolean"
        cell "false"
          code "false"
        cell "snapshot"
          code "snapshot"
      row
        cell "--explain"
          link "--explain"
            code "--explain"
        cell "boolean"
        cell "false"
          code "false"
        cell "diff"
          code "diff"
      row
        cell "--ignore-view-line"
          link "--ignore-view-line"
            code "--ignore-view-line"
        cell "regex (repeatable)"
        cell "none"
        cell "diff"
          code "diff"
      row
        cell "--max-lines"
          link "--max-lines"
            code "--max-lines"
        cell "integer"
        cell "full"
        cell "diff"
          code "diff"
      row
        cell "--max-pages"
          link "--max-pages"
            code "--max-pages"
        cell "integer"
        cell "all"
        cell "diff"
          code "diff"
  heading "Browser & page" (level 2)
  paragraph "Control the browser that renders the page before extraction. Accepted by every browser-driving command — , , , , , , , , , , . takes only the settling flags (, , ) and forces headful."
    link "audit"
      code "audit"
    link "inspect"
      code "inspect"
    link "tree"
      code "tree"
    link "outline"
      code "outline"
    link "tabs"
      code "tabs"
    link "list"
      code "list"
    link "interact"
      code "interact"
    link "click"
      code "click"
    link "type"
      code "type"
    link "focus"
      code "focus"
    link "snapshot"
      code "snapshot"
    link "login"
      code "login"
    link "--wait-until"
      code "--wait-until"
    link "--settle"
      code "--settle"
    link "--timeout"
      code "--timeout"
  heading "--root <selector>" (level 3)
    code "--root <selector>"
  list
    listitem "CSS selector · · only"
      strong "Type:"
      strong "Default:"
      code "body"
      strong "Commands:"
      link "tabs"
        code "tabs"
  paragraph "Scope the tab-order walk to a region or component instead of the whole page."
  paragraph "is the last command that runs , and a selector only means something to an in-page walk. Every other browser command reads Chromium's own accessibility tree, which is whole-document: there is no subtree to narrow to, so they reject with that explanation rather than accepting it and silently doing nothing."
    code "tabs"
    strong "in the page"
    code "--root"
  button "Copy Code"
  code
  paragraph "A project-wide now reaches only . On any other command it is — the config loader is otherwise strict and fail-closed, and hard-erroring would red every CI that set the key, over config that was correct when it was written."
    link "defaults.root"
      code "defaults.root"
    code "tabs"
    strong "warned about on stderr, not an error"
  paragraph "A route's is likewise no longer a scope for or . Those warn once, naming the routes, and keep running: the entry is still how a route is identified, and findings from outside the old subtree are now included."
    link "urls[].rootSelector"
      code "urls[].rootSelector"
    link "audit"
      code "audit"
    link "snapshot"
      code "snapshot"
  heading "--device <name>" (level 3)
    code "--device <name>"
  list
    listitem "Playwright device name · none · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Emulate a device — viewport, user agent, touch — to audit the mobile layout. Can't be combined with ."
    link "--cdp"
      code "--cdp"
  button "Copy Code"
  code
  heading "--viewport <WxH>" (level 3)
    code "--viewport <WxH>"
  list
    listitem "· none · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      code "WIDTHxHEIGHT"
      strong "Default:"
      strong "Commands:"
  paragraph "Explicit viewport size. Must match , e.g. . Can't be combined with ."
    code "^\d+x\d+$"
    code "1280x800"
    link "--cdp"
      code "--cdp"
  heading "--wait-until <state>" (level 3)
    code "--wait-until <state>"
  list
    listitem "· · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot, login"
      strong "Type:"
      code "load | domcontentloaded | networkidle | commit"
      strong "Default:"
      code "load"
      strong "Commands:"
  paragraph "The navigation lifecycle event to wait for before extracting. Use for JS-heavy pages that keep fetching after ."
    code "networkidle"
    code "load"
  heading "--settle <ms>" (level 3)
    code "--settle <ms>"
  list
    listitem "integer ms · · (clamped) · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot, login"
      strong "Type:"
      strong "Default:"
      code "0"
      strong "Max:"
      code "30000"
      strong "Commands:"
  paragraph "Extra wait after the state, for animations or late hydration. Values above the max are clamped; a non-integer is an error."
    link "--wait-until"
      code "--wait-until"
  button "Copy Code"
  code
  heading "--step-settle <ms>" (level 3)
    code "--step-settle <ms>"
  list
    listitem "integer ms · · (clamped) · interact, click, type, focus"
      strong "Type:"
      strong "Default:"
      code "200"
      strong "Max:"
      code "30000"
      strong "Commands:"
  paragraph "How long to wait after each step before looking at the page again. A dispatch returning is not the same as its effect having landed: a React state update flushes on a later tick, a dialog mounts on the next frame. It gates the step's targeting as much as the final diff — a step that opens a menu has to have opened it before the step that clicks an item can find that item."
    emphasis "next"
  button "Copy Code"
  code
  paragraph "opts out and reads immediately. Distinct from , which waits once after the initial page load; this one applies per step."
    code "0"
    link "--settle"
      code "--settle"
  paragraph "A wait, not a guarantee"
  paragraph "Nothing can tell you a page is to navigate. A reaction that lands later than this still won't appear in the diff — raise it for a slow app rather than reading "no changes" as proof that nothing happened."
    emphasis "about"
  heading "--timeout <ms>" (level 3)
    code "--timeout <ms>"
  list
    listitem "integer ms · · · (clamped) · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot, login"
      strong "Type:"
      strong "Default:"
      code "30000"
      strong "Min:"
      code "1"
      strong "Max:"
      code "120000"
      strong "Commands:"
  paragraph "Navigation timeout. Unlike Playwright, is accepted (no "wait forever"); values above the max are clamped."
    code "0"
    strong "not"
  heading "--headful" (level 3)
    code "--headful"
  list
    listitem "boolean · (headless) · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Show the browser window. Can't be combined with . is always headful."
    link "--cdp"
      code "--cdp"
    link "login"
      code "login"
  heading "--cdp <endpoint>" (level 3)
    code "--cdp <endpoint>"
  list
    listitem "CDP endpoint URL · none · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Attach to a running Chrome instead of launching one — the interactive way to audit a login. No emulation over CDP: can't be combined with , , , , or ."
    link "--headful"
      code "--headful"
    link "--device"
      code "--device"
    link "--viewport"
      code "--viewport"
    link "--storage-state"
      code "--storage-state"
    link "--chrome-path"
      code "--chrome-path"
  heading "--chrome-path <file>" (level 3)
    code "--chrome-path <file>"
  list
    listitem "path to a browser executable · none · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Launch this specific browser binary instead of Playwright's bundled Chromium. Can't be combined with — an already-running browser is the browser. Resolution order (shared with the MCP server's ): (error if missing) > env (error if missing) > the cache (silently skipped if absent) > Playwright's own bundled Chromium."
    link "--cdp"
      code "--cdp"
    code "REAL_A11Y_CHROME_PATH"
    code "--chrome-path"
    code "REAL_A11Y_CHROME_PATH"
    link "install"
      code "install"
  button "Copy Code"
  code
  heading "--allow-file" (level 3)
    code "--allow-file"
  list
    listitem "boolean · · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Approve targets, which are blocked by default. Real, but omitted from . A path you type () is normalized to a URL, so this is what unlocks auditing a built file."
    code "file:"
    code "--help"
    code "./dist/index.html"
    code "file:"
  heading "--storage-state <file>" (level 3)
    code "--storage-state <file>"
  list
    listitem "path to a saved session · none · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Audit as a logged-in user, using a session file written by . Can't be combined with . See ."
    link "login"
      code "login"
    link "--cdp"
      code "--cdp"
    link "Authenticated pages"
  button "Copy Code"
  code
  heading "--audit-origin <origin>" (level 3)
    code "--audit-origin <origin>"
  list
    listitem "origin (repeatable) · the target's own origin · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "An extra origin allowed under . Origin pinning stops a redirect from routing extraction to an unintended, cookie-matching origin. Repeat for each additional origin; must parse as a URL."
    link "--storage-state"
      code "--storage-state"
  button "Copy Code"
  code
  heading "Output" (level 2)
  paragraph "Format and destination. / a pipe never suppresses progress — that always stays on stderr."
    code "-o"
  heading "-f, --format <fmt>" (level 3)
    code "-f, --format <fmt>"
  list
    listitem "enum (per command) · first value below · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot, diff"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Never auto-switched — piping only drops color. Allowed values depend on the command:"
  table
    rowgroup
      row
        columnheader "Command"
        columnheader "Values"
        columnheader "Default"
    rowgroup
      row
        cell "audit, inspect, tree, outline, tabs, list"
        cell "pretty | json"
          code "pretty"
          code "json"
        cell "pretty"
          code "pretty"
      row
        cell "diff"
        cell "pretty | json | md"
          code "pretty"
          code "json"
          code "md"
        cell "pretty"
          code "pretty"
      row
        cell "snapshot"
        cell "json | md | sarif | junit | jsonl"
          code "json"
          code "md"
          code "sarif"
          code "junit"
          code "jsonl"
        cell "json"
          code "json"
  paragraph "requires (results anchor to repo file paths)."
    code "sarif"
    link "--config"
      code "--config"
  heading "-o, --output <file>" (level 3)
    code "-o, --output <file>"
  list
    listitem "path · stdout · audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot, diff"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Write the report to a file (progress stays on stderr). A typo'd path fails before the browser launches, not after. For , is the fallback when this is omitted."
    link "snapshot"
      code "snapshot"
    code "A11Y_SNAPSHOT_OUT"
  heading "-q, --quiet" (level 3)
    code "-q, --quiet"
  list
    listitem "boolean · · install, audit, inspect, tree, outline, tabs, list, interact, click, type, focus, snapshot, diff"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Suppress progress lines on stderr. still prints the resolved executable path on stdout."
    link "install"
      code "install"
  heading "--verbose" (level 3)
    code "--verbose"
  list
    listitem "boolean · · all"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Extra diagnostics on stderr (per-page timings, and more)."
  heading "-h, --help" (level 3)
    code "-h, --help"
  list
    listitem "boolean · all"
      strong "Type:"
      strong "Commands:"
  paragraph "Print that command's usage and flags, then exit . with no command prints the command list."
    code "0"
    code "real-a11y --help"
  heading "Config" (level 2)
  paragraph "An seeds any flag you don't pass — the Jest/ESLint model. Accepted by command."
    code "a11y.config.json"
    strong "every"
  heading "--config <file>" (level 3)
    code "--config <file>"
  list
    listitem "path · auto-discovered in cwd · all"
      strong "Type:"
      strong "Default:"
      code "a11y.config.json"
      strong "Commands:"
  paragraph "Point at a config elsewhere. Its block seeds unset flags; its list is the page set for and ( reads two snapshot files, so it only takes from the config). The config is strict and fail-closed — a typo'd key is a hard error, so a mistake can't silently un-gate CI. See ."
    code "defaults"
    code "urls"
    link "audit"
      code "audit"
    link "snapshot"
      code "snapshot"
    code "diff"
    code "defaults"
    link "Configure once"
  heading "--no-config" (level 3)
    code "--no-config"
  list
    listitem "boolean · · all"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Ignore an auto-discovered config for this run."
  heading "Gate" (level 2)
  paragraph "Which rules run, and what fails the build."
  heading "--rules <ids>" (level 3)
    code "--rules <ids>"
  list
    listitem "comma-separated ids · all five · audit, inspect, snapshot"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Run only a subset of the rules. Valid ids: , , , , . An unknown id is a hard error."
    code "no-unlabeled-interactive"
    code "image-alt"
    code "heading-order"
    code "dialog-labeled"
    code "landmark-structure"
  button "Copy Code"
  code
  heading "--fail-on <level>" (level 3)
    code "--fail-on <level>"
  list
    listitem "· (audit, inspect, diff) · (snapshot) · audit, inspect, snapshot, diff"
      strong "Type:"
      code "error | warning | never"
      strong "Default:"
      code "error"
      code "never"
      strong "Commands:"
  paragraph "The gate threshold. exits on any error; also fails on warnings; never fails the build (it still reports). For , only findings at or above the threshold gate — drift and fixes never fail."
    code "error"
    code "1"
    code "warning"
    code "never"
    link "diff"
      code "diff"
    strong "NEW"
  heading "--no-annotate" (level 3)
    code "--no-annotate"
  list
    listitem "boolean · annotate on · audit, inspect"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Skip the GitHub Actions annotations and job-summary report that and emit when running under Actions."
    code "::error"
    link "audit"
      code "audit"
    link "inspect"
      code "inspect"
  heading "Command-specific" (level 2)
  paragraph "Flags that belong to a single command (or a small set)."
  heading "--channel <name>" (level 3)
    code "--channel <name>"
  list
    listitem "· · install"
      strong "Type:"
      code "stable | beta | dev | canary"
      strong "Default:"
      code "stable"
      strong "Commands:"
  paragraph "Which Chrome for Testing release channel to track. Re-resolves the channel's current build over the network; skips the download if it's unchanged from what's already installed. Mutually exclusive with ."
    link "--version"
      code "--version"
  heading "--version <buildId>" (level 3)
    code "--version <buildId>"
  list
    listitem "an exact Chrome build, e.g. · none · install"
      strong "Type:"
      code "131.0.6778.87"
      strong "Default:"
      strong "Commands:"
  paragraph "Pin an exact Chrome for Testing build instead of tracking a channel. Skips the version-resolution network call entirely — the download URL is deterministic — so this is the escape hatch when the Chrome for Testing version endpoint is unreachable. Mutually exclusive with ."
    link "--channel"
      code "--channel"
  heading "--force" (level 3)
    code "--force"
  list
    listitem "boolean · · install"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Reinstall even if the target build is already cached."
  heading "--include-generic" (level 3)
    code "--include-generic"
  list
    listitem "boolean · · ,"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Honored by:"
      link "tree"
        code "tree"
      link "inspect"
        code "inspect"
  paragraph "Include generic container nodes (untyped / wrappers) in the semantic tree, which are collapsed away by default. Only the commands that print a full tree honor it. , , , and accept the flag but ignore it — snapshot's artifact tree always collapses generics."
    code "div"
    code "span"
    link "snapshot"
      code "snapshot"
    link "outline"
      code "outline"
    link "tabs"
      code "tabs"
    link "list"
      code "list"
  heading "--md" (level 3)
    code "--md"
  list
    listitem "boolean · · snapshot"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Shorthand for — a human-readable report instead of the JSON artifact. Conflicts with an explicit non- ."
    link "--format md"
      code "--format md"
    code "md"
    code "--format"
  heading "--baseline <file>" (level 3)
    code "--baseline <file>"
  list
    listitem "path · none · snapshot, diff"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Suppress findings this baseline accepts: they stay in the report (marked ) but drop out of the count (and SARIF). Identity-matched, not string-matched. See ."
    code ""suppressed": true"
    link "--fail-on"
      code "--fail-on"
    link "Adopt the gate on existing debt"
  heading "--update-baseline" (level 3)
    code "--update-baseline"
  list
    listitem "boolean · · snapshot"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Rewrite the baseline from the current findings, then — it writes the baseline file (, or 's path) and never gates. Prints on stderr and carries forward fields on entries that still match."
    strong "stop"
    code ".a11y-baseline.json"
    link "--baseline"
      code "--baseline"
    code "+N new, -N stale"
    code "note"
  button "Copy Code"
  code
  heading "--explain" (level 3)
    code "--explain"
  list
    listitem "boolean · · diff"
      strong "Type:"
      strong "Default:"
      code "false"
      strong "Commands:"
  paragraph "Add a plain-language summary of structural changes ("Heading level changed: h2 → h3", "Keyboard tab stop removed …") to the neutral diff. Opt-in because the statements are an interpretive layer; the default never makes a claim the diff can't back up. Advisory only — it never affects the exit code."
  paragraph "This includes a statement when the element focused at capture time differs between the two snapshots (a moved autofocus target, or focus that appeared or vanished). Because focus isn't structure, it's excluded from the structural diff — a page where focus moved shows no add/remove churn, just this one statement."
    strong
      code "Focused element changed: … → …"
    emphasis "only"
  paragraph "Under the statements are inert — the filter removes the structure axis they summarize (same as ), so a config default of never conflicts with an explicit filter."
    link "--only findings"
      code "--only findings"
    code "--max-lines"
    code "explain: true"
  heading "--only <axis>" (level 3)
    code "--only <axis>"
  list
    listitem "· the full two-axis report · diff, snapshot"
      strong "Type:"
      code "findings | views"
      strong "Default:"
      strong "Commands:"
  paragraph "Report just one axis: (the accessibility problems) or (the tree/outline/tab-order structure). An — the exit gate is computed from the full findings either way, so a filtered CI run can exit non-zero while showing only structure. What explains a gating exit: on , the always-present one-line findings summary; on , a stderr note () — the views-only report itself stays a pure structure export with no findings content at all."
    code "--only findings"
    code "--only views"
    strong "output filter"
    code "diff"
    code "snapshot"
    code "real-a11y: gate: …"
  paragraph "An enum on purpose: contradictory states are unrepresentable, and a config default () is overridable from the command line by passing the other value."
    code ""defaults": { "only": "findings" }"
  list
    listitem "— hides the view hunks; view-axis modifiers (, , ) become inert. hides per-finding entries; composes with . In the filtered axis's arrays are omitted (/ vs //); the summary and per-page boolean always ship."
      strong "diff"
      code "--only findings"
      code "--explain"
      code "--max-lines"
      code "--ignore-view-line"
      code "--only views"
      link "--explain"
        code "--explain"
      code "--format json"
      code "views"
      code "structural"
      code "new"
      code "changed"
      code "removed"
      code "structuralDiff"
    listitem "— shapes the , or writes a : the filtered axis is stripped and records the capture mode. A partial artifact is a machine export (smaller payload, custom tooling), — rejects it outright, because an empty-because-filtered axis is indistinguishable from empty-because-clean and would read as everything-new or all-removed. // are findings-shaped by construction and reject the flag."
      strong "snapshot"
      strong "report"
        code "--format md"
      strong "partial artifact"
        code "--format json"
      code "meta.only"
      strong "not a diffable snapshot"
      code "diff"
      code "sarif"
      code "junit"
      code "jsonl"
  button "Copy Code"
  code
  heading "--ignore-view-line <regex>" (level 3)
    code "--ignore-view-line <regex>"
  list
    listitem "JS regex (repeatable) · none · diff"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Drop view lines matching the pattern before diffing — for generated content that differs on every build (a "last updated" timestamp, a build hash) and would otherwise read as drift on every page. Each value must be a valid ."
    code "RegExp"
  button "Copy Code"
  code
  heading "--max-lines <n>" (level 3)
    code "--max-lines <n>"
  list
    listitem "positive integer · full · diff"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Cap the structural diff to lines per page, then . For CI comments — run once uncapped to a log and once capped to the comment."
    emphasis "n"
    code "… N more"
  heading "--max-pages <n>" (level 3)
    code "--max-pages <n>"
  list
    listitem "positive integer · all · diff"
      strong "Type:"
      strong "Default:"
      strong "Commands:"
  paragraph "Detail at most changed pages, then list the rest by name. For CI comments."
    emphasis "n"
  button "Copy Code"
  code
  heading "--save <file>" (level 3)
    code "--save <file>"
  list
    listitem "path · none () · login"
      strong "Type:"
      strong "Default:"
      strong "required"
      strong "Commands:"
  paragraph "Where writes the captured session. Written (POSIX); the command warns if the path sits un-gitignored inside a repo. Session storage isn't captured — apps that keep auth there need instead."
    link "login"
      code "login"
    code "0o600"
    link "--cdp"
      code "--cdp"
  button "Copy Code"
  code
contentinfo
  link "Edit this page on GitHub"
  paragraph "Last updated:"
    time "[redacted]"
  navigation "Pager"
    link "Previous page@real-a11y-dev/cli"
    link "Next pageConfiguration"