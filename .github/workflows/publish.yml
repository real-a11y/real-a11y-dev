name: Publish to npm

# Triggered manually or by pushing a version tag (e.g. `v0.1.1-beta.0`).
# Publishes every public workspace package with npm provenance.
#
# Auth: NPM_TOKEN (granular automation token) for the publish itself,
# OIDC for the provenance attestation.
#
# Trusted Publisher (npm OIDC for the publish auth path) is configured on
# every @real-a11y-dev/* package and works for the provenance signing
# step, but pnpm 9.x + Node 20's npm 10.x don't fully wire OIDC into the
# `PUT /<pkg>` publish call yet — they fall through to the .npmrc
# `_authToken` (which `actions/setup-node` reads from `NODE_AUTH_TOKEN`).
# Without the token the publish 404s after provenance succeeds. Once
# pnpm 10.x + npm 11.5+ are on the runner we can drop NODE_AUTH_TOKEN
# again and rely purely on Trusted Publisher.
#
# Trusted Publisher binding to keep in place per package:
#   npmjs.com/package/@real-a11y-dev/<name>/access
#   → Publishing access → GitHub Actions → Add Trusted Publisher
#     - Organization or user: real-a11y
#     - Repository:           real-a11y-dev
#     - Workflow filename:    publish.yml
#     - Environment name:     (blank)
#
# Provenance attestations land in the public Sigstore transparency log
# regardless of the publish-auth path and show as a verified badge on
# the package page.

on:
  workflow_dispatch:
    inputs:
      tag:
        # Leave blank to derive from the version (prerelease → `beta`, otherwise
        # → `latest`). Set it only to force a channel. Default is intentionally
        # empty, not `beta`: `release-tag.yml` dispatches without this input, so
        # a `beta` default would publish a stable graduation under `beta`.
        description: "npm dist-tag to publish under — blank = derive from version"
        required: false
        default: ""
  # Manual break-glass: pushing a `v*` tag by hand still publishes. The
  # automation uses per-package tags + an explicit dispatch instead, so this
  # path is only for recovering a release by hand.
  push:
    tags:
      - "v*"

# Default to read-only for any token derived from this workflow. The
# publish job below escalates to `id-token: write` for npm OIDC and keeps
# `contents: read` — least-privilege per OpenSSF Scorecard's
# Token-Permissions check.
permissions: read-all

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    outputs:
      # Set by the Publish step itself, deliberately NOT derived from this job's
      # conclusion. `Advance latest dist-tag` runs after it and exits non-zero
      # on a transient npm error — but the packages are on npm either way, so
      # the docs still have to catch up. Keying the deploy on the job would
      # freeze real-a11y.dev on the previous release with nothing reporting it.
      published: ${{ steps.publish.outputs.published }}
      # Whether `latest` ends up pointing at this publish — see `Resolve
      # dist-tag`. The stable docs deploy on this, not on the channel name.
      is_latest: ${{ steps.tag.outputs.is_latest }}
    permissions:
      # Required for OIDC: lets npm validate the GitHub Actions identity
      # against each package's Trusted Publisher config, and lets npm
      # publish attach a provenance attestation.
      id-token: write
      contents: read
    steps:
      # No ref override: the run is dispatched at (or the break-glass `v*` tag is
      # pushed at) the release commit, so the default checkout — GITHUB_SHA — and
      # npm's provenance attestation, which is built from GITHUB_SHA/OIDC claims
      # rather than the working tree, both resolve to that exact commit.
      # `release-tag.yml` dispatches at a release tag precisely so these agree.
      - uses: actions/checkout@v4

      # pnpm version is read from `packageManager` in package.json.
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - name: Install
        run: pnpm install --frozen-lockfile

      # Root `build` (the same one CI and the pre-push hook run) builds every
      # publishable package; it shares its workspace filter with `typecheck`
      # and `test` below, so the gate can never trip over an unbuilt package.
      - name: Build all packages
        run: pnpm build

      # A version tag can be pushed from any commit, and the build above is
      # `tsup`/`vite` — it transpiles without type-checking and never runs the
      # test suites. So a type error or a failing test in the tagged commit
      # would publish to npm silently. Re-run the two gates a build can't catch
      # on the exact release commit, before anything is published. (format:check
      # and lint are already enforced on `main` by the required `test` check, so
      # repeating them here would only add cost and failure surface.)
      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      # Derive the channel from changesets pre-mode instead of hardcoding `beta`.
      # `release-tag.yml` dispatches *without* a `tag` input and a `v*` tag-push
      # carries none either, so an empty input means "decide from pre-mode".
      #
      # The signal is `.changeset/pre.json` `mode` — the same repo-global flag
      # `advance-latest.mjs` keys off, so the two never disagree. In pre-mode
      # (`mode == "pre"`) every version `changeset version` produces is a
      # prerelease → `beta` (advance-latest.mjs then moves `latest` per package);
      # once `changeset pre exit` has run, every one is stable → `latest`.
      # Hardcoding `beta` would ship a stable graduation under `beta` and strand
      # `latest` on the last prerelease (advance-latest.mjs no-ops out of
      # pre-mode). Reading core's *version string* instead would misfire in a
      # legitimate changesets sequence — `pre exit` graduating core to `0.1.0`,
      # then `pre enter` with a changeset touching only cli — where core reads
      # stable but the cli prerelease being published wants `beta`; pre-mode is
      # correct for whatever is actually published, since in pre-mode nothing
      # graduates. Pass the `tag` input explicitly to force a channel.
      - name: Resolve dist-tag
        id: tag
        run: |
          MODE="$(node -p "try { require('./.changeset/pre.json').mode } catch { 'exit' }")"
          TAG="${{ github.event.inputs.tag }}"
          if [ -z "$TAG" ]; then
            if [ "$MODE" = "pre" ]; then TAG="beta"; else TAG="latest"; fi
          fi
          echo "value=$TAG" >> "$GITHUB_OUTPUT"

          # Does `latest` end up pointing at THIS publish? That, not the channel
          # name, is what decides whether the stable docs may deploy — the site
          # describes what `npm install <pkg>` delivers, which is whatever
          # `latest` resolves to.
          #
          # True two ways: the channel IS `latest`; or the channel is `beta` in
          # prerelease mode, because `Advance latest dist-tag` below then moves
          # `latest` onto the just-published prereleases. Gating on
          # `TAG = latest` alone would be wrong for the whole pre-1.0 period —
          # it would freeze real-a11y.dev until `changeset pre exit`, even
          # though the betas are exactly what a reader installs.
          #
          # The case this exists to catch is forcing `tag: beta` OUTSIDE
          # pre-mode: advance-latest.mjs no-ops there (its own pre-mode guard),
          # so `latest` stays on the previous stable while the site would
          # otherwise jump to the beta commit — npm and the docs describing
          # different code, which is the drift this whole gate exists to remove.
          if [ "$TAG" = "latest" ] || { [ "$TAG" = "beta" ] && [ "$MODE" = "pre" ]; }; then
            echo "is_latest=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_latest=false" >> "$GITHUB_OUTPUT"
          fi

      # pnpm's `publish -r` publishes every package whose version/tag is new,
      # skips the rest. `--no-git-checks` lets tag-triggered runs proceed even
      # when the working tree does not match main exactly.
      #
      # NODE_AUTH_TOKEN feeds the .npmrc `_authToken=${NODE_AUTH_TOKEN}` line
      # that `actions/setup-node` writes; pnpm reads it from there for the
      # `PUT /<pkg>` call. NPM_CONFIG_PROVENANCE keeps provenance attestation
      # signing on (uses GHA OIDC under the hood — independent of the
      # publish-auth path).
      - name: Publish
        id: publish
        env:
          NPM_CONFIG_PROVENANCE: "true"
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          pnpm publish -r \
            --access public \
            --tag ${{ steps.tag.outputs.value }} \
            --no-git-checks
          echo "published=true" >> "$GITHUB_OUTPUT"

      # After a prerelease publish, advance `latest` to the just-published
      # versions. `--tag beta` never moves `latest`, so pre-1.0 `npm install
      # <pkg>` would keep resolving an old version and the npm page would
      # headline a stale one. The script self-guards: it no-ops when the publish
      # already targeted `latest`, and when the repo is past changesets
      # prerelease mode — so it can never regress a stable `latest` to a beta.
      - name: Advance latest dist-tag (prerelease only)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: node scripts/advance-latest.mjs --published-tag ${{ steps.tag.outputs.value }}

  # The stable docs are the last link in the release chain: real-a11y.dev serves
  # the RELEASED docs, so it can only move once the packages are on npm.
  #
  # An explicit dispatch, not a `workflow_run` listener in docs.yml — see that
  # file's header for the full reasoning. Short version: this run is itself
  # created by `release-tag.yml` with GITHUB_TOKEN, and a run created that way
  # does not raise events that start further runs. `workflow_dispatch` is the
  # documented exception; `workflow_run` is not, so a listener would never fire
  # here and would do it silently.
  #
  # Its own job so `actions: write` is never granted to the job holding
  # NPM_TOKEN, and so the two never share a runner.
  deploy-docs:
    needs: publish
    # `always()` on purpose: a failure in a post-publish chore must not strand
    # the docs on the previous release. `published` is what decides, and it is
    # set by the publish step rather than inferred from the job.
    #
    # `is_latest` is the second half: the stable site describes what `npm
    # install` delivers, so it may only move when this publish is what `latest`
    # now resolves to. In prerelease mode that includes a `beta` publish, since
    # advance-latest.mjs moves `latest` onto it.
    if: >-
      always() && needs.publish.outputs.published == 'true' &&
      needs.publish.outputs.is_latest == 'true'
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    steps:
      # Dispatched at `main` so the `github-pages` environment sees an allowed
      # branch; the released commit travels as an input and is what docs.yml
      # checks out. docs.yml refuses a sha that carries no release tag.
      - name: Deploy the released docs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run docs.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref main \
            -f sha="$GITHUB_SHA"
