---
---

No release. Prettier 3.9 reformats three union-type lines in published packages (`browser`, `core`, `extension`) as part of the minor-and-patch dependency bump. Behavior unchanged — present only to satisfy the `changeset` CI gate on `packages/*/src/` edits.
