---
---

No release. Import-order autofix only (`eslint --fix`) — a pure reorder of import statements with no change to behavior, types, or emitted output, so nothing to tell consumers. Empty changeset to satisfy the `changeset` CI gate, which fires on any `packages/*/src/` edit to a published package.
