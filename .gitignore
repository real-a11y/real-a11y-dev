node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
.env.*
coverage/
**/.vitepress/cache/
**/.vitepress/dist/
a11y-snapshots.md
a11y.diff

# playwright run artifacts (per-package)
test-results/
playwright-report/

# storybook build output (per-package)
storybook-static/

# Claude Code settings stay user-specific (launch.json, settings.local.json),
# but committed skills under .claude/skills/ are shared with the team.
.claude/*
!.claude/skills/
