export interface ToastExampleProps {
  /** Visible label of the trigger button. */
  trigger: string;
  /** Title shown in the toast and used as its accessible name. */
  title: string;
  /** Optional description shown below the title. */
  description?: string;
}
