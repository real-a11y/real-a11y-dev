export interface DialogNestedExampleProps {
  /** Visible label of the outer trigger button. */
  outerTrigger: string;
  /** Title shown at the top of the outer dialog. */
  outerTitle: string;
  /** Label of the button inside the outer dialog that opens the inner dialog. */
  innerTrigger: string;
  /** Title shown at the top of the inner dialog. */
  innerTitle: string;
}
